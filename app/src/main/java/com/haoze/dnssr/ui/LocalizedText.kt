package com.haoze.dnssr.ui

import android.content.Context
import androidx.compose.runtime.Composable
import androidx.compose.ui.platform.LocalConfiguration

private fun stableTextResourceName(text: String): String {
    var hash = 0
    text.forEach { character -> hash = 31 * hash + character.code }
    return "localized_text_${hash.toLong().and(0x7fffffff)}"
}

private fun translateEnglish(text: String, context: Context): String {
    val resourceId = context.resources.getIdentifier(
        stableTextResourceName(text),
        "string",
        context.packageName
    )
    if (resourceId != 0) return context.getString(resourceId)

    return when {
        text == "同意并继续" -> "Agree and continue"
        text == "不同意并退出" -> "Decline and exit"
        text == "Go 隧道用于在本机 VPN 中接管 TCP、UDP、DNS 和 HTTP(S) 流量，使谛听能识别应用连接。" -> "The Go tunnel takes over TCP, UDP, DNS, and HTTP(S) traffic in the local VPN so that DNSSR can identify app connections."
        text == "当前触发功能：" -> "Triggered features:"
        text == "启用期间，智能选择和最快响应解析模式不可用，只能使用单一服务或依次尝试模式。" -> "While enabled, smart selection and fastest-response modes are unavailable; use single-provider or primary-backup mode instead."
        text == "有效缓存" -> "Valid cache entries"
        text == "白名单规则" -> "Allowlist rules"
        text.contains(" 次；第 N+1 次开始升级") -> text.replace(" 次；第 N+1 次开始升级", " attempts; escalation starts at N+1")
        text.contains(" 秒；首次升级起固定计时") -> text.replace(" 秒；首次升级起固定计时", " seconds; timing starts at the first escalation")
        text == "缓存命中的 DNS 请求不会触发实际竞速，因此不计入这里的统计。" -> "DNS requests served from cache do not trigger an actual race and are excluded from these statistics."
        text == "移除已缓存的解析结果，下次访问会重新查询" -> "Remove cached resolution results; the next visit will query again"
        text == "清除智能选择的健康样本，让它重新按默认权重分配流量" -> "Clear smart-selection health samples and redistribute traffic using default weights"
        text == "清除 Bootstrap DNS 解析健康样本，重新按默认权重选择" -> "Clear Bootstrap DNS health samples and select using default weights again"
        text == "清除 DNS 的域名屏蔽、白名单和覆写规则" -> "Clear DNS domain block, allowlist, and override rules"
        text == "清除 Go 隧道的域名、URL、白名单和覆写规则" -> "Clear Go tunnel domain, URL, allowlist, and override rules"
        text == "阻断所选应用的 QUIC，促使其回退到可检查的 TCP；部分站点可能加载失败" -> "Block QUIC for selected apps so they fall back to inspectable TCP; some sites may fail to load"
        text == "仅阻断所选应用的 DNS-over-TLS（DoT/TCP 853），防止绕过域名规则" -> "Block DNS-over-TLS (DoT/TCP 853) only for selected apps to prevent bypassing domain rules"
        text == "查看 HTTPS 流量检查的逐请求结果和 HTTPS 检查自动旁路记录" -> "View per-request HTTPS inspection results and automatic bypass records"
        text == "使用前说明" -> "Before you begin"
        text == "此功能不适合没有相关经验的用户。安装、卸载或重新安装 CA 证书需要一定操作能力，操作不当可能导致部分应用无法联网；仅在你能自行处理这些问题时使用。" -> "This feature is not suitable for users without relevant experience. Installing, removing, or reinstalling a CA certificate requires care; mistakes may prevent some apps from connecting. Use it only if you can handle these issues yourself."
        text == "开启后，Go 隧道会接管流量，但仅检查明确选择的应用；其他应用直接转发。HTTPS 仅在应用信任 HTTPS 检查根证书且未使用证书固定或自定义校验时才能解密。" -> "When enabled, the Go tunnel takes over traffic but inspects only explicitly selected apps; other apps are forwarded directly. HTTPS can be decrypted only when an app trusts the HTTPS inspection root certificate and does not use certificate pinning or custom validation."
        text == "不兼容的连接会作为 HTTPS 检查自动旁路直接转发。HTTP/3（QUIC）默认直连；开启“尝试检查 HTTP/3”后，会阻断所选应用的 UDP 443，促使支持回退的客户端改用 TCP。" -> "Incompatible connections are forwarded directly as automatic HTTPS inspection bypasses. HTTP/3 (QUIC) is direct by default; enabling “Try to inspect HTTP/3” blocks UDP 443 for selected apps so clients that support fallback can use TCP."
        text == "HTTP(S) 流量检查需要 Android 10 或更高版本，当前设备不满足运行条件，因此本页功能无法启用，谛听将继续使用 DNS-only 模式。DNS 解析、域名规则和其他基础功能不会受到影响，也不需要安装根证书。若以后升级到受支持的系统，请在启用前了解证书安装、应用信任和 HTTPS 解密的限制；配置不当可能导致部分应用无法联网，证书固定或自定义校验的连接也可能无法被检查。" -> "HTTP(S) inspection requires Android 10 or later. This device does not meet the requirements, so this page cannot enable the feature and DNSSR will continue in DNS-only mode. DNS resolution, domain rules, and other core features are unaffected, and no root certificate is required. If you later upgrade to a supported system, understand certificate installation, app trust, and HTTPS decryption limits before enabling it; incorrect configuration may prevent some apps from connecting, and pinned or custom-validated connections may still be uninspectable."
        text == "启用所选应用的 HTTPS 流量检查" -> "Enable HTTPS inspection for selected apps"
        text == "尝试检查 HTTP/3" -> "Try to inspect HTTP/3"
        text == "阻止检查应用使用加密 DNS" -> "Block inspected apps from using encrypted DNS"
        text == "启用智能选择或最快响应并产生真实 DNS 查询后，这里会显示各策略的表现。" -> "Strategy performance appears here after smart selection or fastest response generates real DNS queries."
        text == "排除后，应用将使用系统 DNS，不参与本应用的过滤、缓存、日志和统计。" -> "Excluded apps use the system DNS and do not participate in this app's filtering, caching, logging, or statistics."
        text == "感谢每一位为谛听提出建议、帮助测试的共建者！名单按用户名称的字母顺序排列，中文名称按拼音排序。" -> "Thanks to every DNSSR co-builder who contributed suggestions and testing help. The list is sorted alphabetically by username, with Chinese names sorted by pinyin."
        text == "阿里云" -> "Alibaba Cloud"
        text == "腾讯云 DNSPod" -> "Tencent Cloud DNSPod"
        text == "仅影响阿里云和 DNSPod 的内置服务；四种解析模式中已选择的对应预设服务会同步切换协议。" -> "Only built-in Alibaba Cloud and DNSPod services are affected; the corresponding preset providers selected in all four resolution modes switch protocol together."
        text.startsWith("原始 TTL ") -> text
            .replace("原始 TTL ", "Original TTL ")
            .replace(" · 命中 ", " · Hits ")
            .replace(" 次 · ", " times · ")
        text.startsWith("最后命中 ") -> text.replace("最后命中 ", "Last hit ").replace("无", "none")
        text.contains(" · 平均胜出耗时 ") -> text.replace(" · 平均胜出耗时 ", " · Average winning latency ")
        text.startsWith("首选命中 ") -> text.replace("首选命中 ", "Primary wins ").replace(" · 平均首选耗时 ", " · Average primary latency ")
        text.startsWith("成功 ") && text.contains(" · 首选命中 ") -> text
            .replace("成功 ", "Success ")
            .replace(" · 首选命中 ", " · Primary wins ")
            .replace(" · 兜底 ", " · Fallback ")
            .replace(" 次", " times")
        text.contains(" · 样本 ") -> text
            .replace("成功 ", "Success ")
            .replace(" · 平均 ", " · Average ")
            .replace(" · 备用 ", " · Fallback ")
            .replace(" · 样本 ", " · Samples ")
            .replace(" 次", " times")
        text.contains(" 次 · ") -> text.replace(" 次 · ", " attempts · ")
        text.contains(" 次成功") -> text.replace(" 次成功", " successful")
        text.startsWith("全部失败（") -> text.replace("全部失败（", "All failed (").replace(" 次）", " attempts)")
        text.contains(" · ") && text.contains(" 后过期") -> text
            .replace(" 小时", " hours")
            .replace(" 分钟", " minutes")
            .replace(" 秒", " seconds")
            .replace(" 后过期", " until expiry")
        text == "需要应用列表访问权限才能选择应用。未授权不会影响其他功能。" -> "App-list access is required to choose apps. Denying it does not affect other features."
        text == "支付宝付款码" -> "Alipay payment QR code"
        text.startsWith("感谢您对谛听项目的赞助支持") -> "Thanks for supporting the DNSSR project"
        text.startsWith("感谢为谛听提出建议与帮助测试") -> "Thanks for suggestions and testing help for DNSSR"
        text.endsWith("的头像") -> text.removeSuffix("的头像") + "'s avatar"
        text == "微信付款码" -> "WeChat payment QR code"
        text == "你的每一笔支持都会用于" -> "Your support helps us"
        text == "持续开发新功能" -> "Continue developing new features"
        text == "修复 Bug" -> "Fix bugs"
        text == "购买词元" -> "Purchase token credits"
        text == "即使不捐赠，也欢迎" -> "Even without donating, you are welcome to"
        text == "点一个 Star⭐" -> "Star the project ⭐"
        text == "提交 Issue" -> "Submit an issue"
        text == "提交 PR" -> "Submit a pull request"
        text == "分享给更多人" -> "Share it with others"
        text == "头像均已缓存，无需刷新" -> "All avatars are cached; no refresh needed"
        text == "刷新头像" -> "Refresh avatars"
        text == "当前按赞助时间由晚到早排列" -> "Currently sorted from newest to oldest sponsorship"
        text == "当前按赞助时间由早到晚排列" -> "Currently sorted from oldest to newest sponsorship"
        text == "当前按赞助时间由晚到早排列，点击切换为由早到晚" -> "Currently newest first; tap to switch to oldest first"
        text == "当前按赞助时间由早到晚排列，点击切换为由晚到早" -> "Currently oldest first; tap to switch to newest first"
        text == "暂时还没有赞助者，期待在这里写下你的名字。" -> "No sponsors yet. We look forward to seeing your name here."
        text.startsWith("已刷新 ") && text.contains(" 个头像") -> text.replace("已刷新 ", "Refreshed ").replace(" 个头像", " avatars")
        text == "导入规则..." -> "Importing rules..."
        text.contains(" / ") && text.startsWith("正在导入规则...") -> text.replace("正在导入规则...", "Importing rules...")
        text == "导入 DNS 过滤订阅文件" -> "Import DNS filtering subscription file"
        text == "创建不可更新的本地 DNS 过滤订阅" -> "Create a local DNS filtering subscription that cannot be updated"
        text == "导入 hosts 覆写订阅文件" -> "Import hosts override subscription file"
        text == "创建不可更新的本地 hosts 覆写订阅" -> "Create a local hosts override subscription that cannot be updated"
        text == "导入 HTTPS 过滤订阅文件" -> "Import HTTPS filtering subscription file"
        text == "创建不可更新的本地 HTTPS 过滤订阅" -> "Create a local HTTPS filtering subscription that cannot be updated"
        text == "导入 HTTPS 规则备份 JSON" -> "Import HTTPS rule backup JSON"
        text == "恢复为手动规则，不创建订阅" -> "Restore as manual rules without creating a subscription"
        text == "本地订阅文件导入后无法更新，可在规则订阅中重命名、启用、禁用或删除。" -> "Imported local subscription files cannot be updated. Rename, enable, disable, or delete them under Rule subscriptions."
        text == "DNS 规则" -> "DNS rules"
        text == "HTTPS 规则" -> "HTTPS rules"
        text == "仅导出当前生效的规则。DNS 使用可订阅的 TXT，HTTPS 使用可完整恢复的备份 JSON。" -> "Only currently active rules are exported. DNS uses subscribable TXT, while HTTPS uses a fully restorable backup JSON."
        text.startsWith("导出 DNS ") -> text.replace("导出 DNS ", "Export DNS ").replace("订阅规则", "subscription rules").replace("手动添加规则", "manually added rules").replace("全部规则", "all rules")
        text.startsWith("导出 HTTPS ") -> text.replace("导出 HTTPS ", "Export HTTPS ").replace("订阅规则", "subscription rules").replace("手动添加规则", "manually added rules").replace("全部规则", "all rules")
        text.startsWith("导出所有订阅导入的当前生效规则") -> text.replace("导出所有订阅导入的当前生效规则", "Export all currently active subscription-imported rules")
        text.startsWith("导出所有手动添加的当前生效规则") -> text.replace("导出所有手动添加的当前生效规则", "Export all currently active manually added rules")
        text.startsWith("合并订阅导入与手动添加的当前生效规则") -> text.replace("合并订阅导入与手动添加的当前生效规则", "Combine currently active subscription-imported and manually added rules")
        text == "导出 CSV" -> "Export CSV"
        text == "选择状态" -> "Choose status"
        text == "请求正常放行，未命中拦截规则" -> "Request passed normally without matching a blocking rule"
        text == "请求命中了覆写规则，并返回覆写后的结果" -> "The request matched an override rule and returned the overridden result"
        text == "请求命中了过滤规则并被拦截" -> "The request matched a filtering rule and was blocked"
        text == "请求解析或处理失败" -> "Request resolution or processing failed"
        text == "请求未被读取或过滤，直接建立连接" -> "The request was not read or filtered; the connection was established directly"
        text.contains(" · 命中缓存") -> text.replace(" · 命中缓存", " · Cache hit")
        text == "未取得 authority" -> "Authority unavailable"
        text.startsWith("匹配规则 · ") -> text.replace("匹配规则 · ", "Matched rule · ")
        text == "管理服务..." -> "Manage providers..."
        text == "服务显示..." -> "Provider visibility..."
        text == "未选择服务商" -> "No provider selected"
        text.startsWith("已选 ") && text.contains(" 个：") -> text.replace("已选 ", "Selected ").replace(" 个：", ": ").replace(" 等", " etc.")
        text == "Go 隧道 · 开启" -> "Go tunnel · On"
        text == "Go 隧道 · 关闭" -> "Go tunnel · Off"
        text == "关闭后将停用以下依赖 Go 隧道的功能：" -> "Disabling this will turn off the following features that depend on the Go tunnel:"
        text == "如果 VPN 正在运行，服务将自动重连。" -> "If the VPN is running, the service will reconnect automatically."
        text == "未检测到 QQ，请搜索群号 1090225658 加入。" -> "QQ was not found. Search for group 1090225658 to join."
        text == "连接中" -> "Connecting"
        text == "断开" -> "Disconnect"
        text == "记录全部请求" -> "Recording all requests"
        text == "仅记录拦截与错误" -> "Recording blocked and failed requests only"
        text == "请求日志已关闭" -> "Request logging is off"
        text.startsWith("记录全部请求 · 更新于 ") -> text.replace("记录全部请求 · 更新于 ", "Recording all requests · Updated on ")
        text.startsWith("仅记录拦截与错误 · ") -> text.replace("仅记录拦截与错误 · ", "Recording blocked and failed requests only · ")
        text.startsWith("请求日志已关闭 · ") -> text.replace("请求日志已关闭 · ", "Request logging is off · ")
        text == "今日请求" -> "Today's requests"
        text == "缓存命中" -> "Cache hits"
        text == "今日流量结构" -> "Today's traffic breakdown"
        text == "实时汇总" -> "Live summary"
        text == "竞速请求" -> "Race requests"
        text == "Bootstrap 尝试" -> "Bootstrap attempts"
        text == "失败" -> "Failed"
        text == "缓存" -> "Cache"
        text == "竞速成功率" -> "Race success rate"
        text == "Bootstrap 成功率" -> "Bootstrap success rate"
        text == "拦截率" -> "Block rate"
        text == "缓存率" -> "Cache rate"
        text == "缓存热点" -> "Cache hotspots"
        text == "竞速胜出" -> "Race winners"
        text.startsWith("平均胜出耗时 ") -> text.replace("平均胜出耗时 ", "Average winning latency ")
        text.contains(" · 成功 ") -> text.replace(" · 成功 ", " · Success ")
            .replace(" · 平均 ", " · Average ")
            .replace(" · 备用 ", " · Fallback ")
            .replace(" · 权重 ", " · Weight ")
            .replace(" · 样本 ", " · Samples ")
        text.contains(" · 占全部请求 ") -> text.replace(" · 占全部请求 ", " · Share of all requests ")
        text == "重点请求概览" -> "Priority request overview"
        text == "仅汇总 DNS 与 HTTPS 的拦截、错误和旁路记录。" -> "Only blocked, failed, and bypassed DNS and HTTPS records are summarized."
        text == "今日拦截" -> "Today's blocked"
        text == "今日错误" -> "Today's errors"
        text == "今日旁路" -> "Today's bypassed"
        text == "最近重点请求" -> "Recent priority requests"
        text == "订阅拦截" -> "Subscription blocking"
        text == "解析运行状态" -> "Resolution runtime status"
        text == "日志记录已暂停" -> "Logging is paused"
        text == "当前不会保存 DNS 或 HTTPS 请求记录，缓存和解析服务仍正常运行。" -> "DNS and HTTPS request records are not currently saved; caching and resolution continue to work."
        text == "当前有效条目" -> "Current valid entries"
        text == "累计缓存命中" -> "Total cache hits"
        text == "关闭竞速模式时，首页解析服务下拉框仅显示这里选中的服务。当前正在使用的服务会始终保留。" -> "When race mode is disabled, the home provider selector shows only the services selected here. The provider currently in use is always kept."
        text == "DNS 请求日志" -> "DNS request logs"
        text == "悬浮窗" -> "Floating window"
        text == "悬浮窗日志" -> "Floating-window logs"
        text == "VPN 运行且应用在后台时显示悬浮球，点击查看最近请求" -> "Show a floating button while the VPN runs in the background; tap it to view recent requests"
        text == "需要系统悬浮窗权限；回到应用前台时悬浮窗会自动隐藏。" -> "System floating-window permission is required; the window hides automatically when the app returns to the foreground."
        text == "关闭“通知常驻”只会停止 VPN 未运行时的监控提醒。VPN 正在运行时，系统要求的前台服务通知会继续显示。" -> "Disabling “Persistent notification” stops monitoring reminders only while the VPN is not running. The required foreground-service notification remains while the VPN is running."
        text == "会向已选择的测速服务商查询这个域名，每个服务商连续测 3 次并按成功样本取平均值。" -> "The selected test providers query this domain three times each; the average is calculated from successful samples."
        text == "可选择 1 个或多个服务商；这里的选择只用于 DNS 查询测速，不影响竞速模式。" -> "Choose one or more providers. This selection is used only for DNS query tests and does not affect race mode."
        text == "测速结果按成功优先、平均耗时从低到高排序；结果只反映当前网络状态。" -> "Test results are sorted by success first and then by average latency; they reflect only the current network state."
        text == "HTTP CONNECT 不支持 UDP；UDP 会被严格阻断，QUIC 可回退到 TCP。代理应用本身将绕过 DNSSR。" -> "HTTP CONNECT does not support UDP; UDP is strictly blocked, and QUIC can fall back to TCP. The proxy app itself bypasses DNSSR."
        text == "SOCKS5 通过 UDP ASSOCIATE 转发 UDP。代理应用本身将绕过 DNSSR，防止流量环路。" -> "SOCKS5 forwards UDP through UDP ASSOCIATE. The proxy app itself bypasses DNSSR to prevent traffic loops."
        text == "已忽略电池优化" -> "Battery optimization ignored"
        text == "允许应用在后台稳定运行" -> "Allow the app to run reliably in the background"
        text == "已忽略" -> "Ignored"
        text == "可设置 1 至 168 小时" -> "Set between 1 and 168 hours"
        text == "请输入 1 至 168 之间的小时数" -> "Enter a number of hours between 1 and 168"
        text == "编辑 DNS 服务商" -> "Edit DNS provider"
        text == "显示该类型的全部服务" -> "Show all services of this type"
        text == "不显示该类型的服务" -> "Do not show services of this type"
        text.startsWith("已选择 ") && text.endsWith(" 个服务") -> text.removePrefix("已选择 ").removeSuffix(" 个服务") + " services selected"
        text == "保存通过、拦截和错误请求" -> "Save passed, blocked, and failed requests"
        text == "减少数据库写入，同时保留关键记录" -> "Reduce database writes while keeping important records"
        text == "不创建或写入 DNS 请求日志" -> "Do not create or write DNS request logs"
        text == "正在加载统计数据" -> "Loading statistics"
        text == "DNS 与 HTTPS 日志合计，含通过、屏蔽和失败" -> "Combined DNS and HTTPS logs, including passed, blocked, and failed requests"
        text == "正在加载" -> "Loading"
        text == "暂无屏蔽订阅" -> "No blocking subscriptions"
        text.endsWith(" 小时") && text.startsWith("每 ") -> "Every " + text.removePrefix("每 ").removeSuffix(" 小时") + " hours"
        text.startsWith("暂无 ") && text.endsWith(" DNS 服务商。") -> text.replace("暂无 ", "No ").replace(" DNS 服务商。", " DNS providers.")
        text == "自动清理时间" -> "Automatic cleanup interval"
        text.startsWith("保留 ") && text.endsWith(" 天") -> text.removePrefix("保留 ").removeSuffix(" 天") + " days"
        text.startsWith("暂无 ") && text.contains(" 自定义服务商。") -> text.replace("暂无 ", "No custom ").replace(" 自定义服务商。点击右上角“新增”添加自己的 DNS 服务。", " providers. Tap “Add” above to add your own DNS service.")
        text == "选择检查应用" -> "Select apps to inspect"
        text == "Go 隧道接管流量后，HTTPS 流量检查仅检查所选应用的 HTTP(S) 请求，其他应用直接转发。选择应用会取消其“排除应用”状态。" -> "After the Go tunnel takes over traffic, HTTPS inspection checks HTTP(S) requests only for selected apps; other apps are forwarded directly. Selecting an app removes it from excluded apps."
        text == "动态策略开启时会临时覆盖以上选择：先返回 NODATA，高频请求后返回 NXDOMAIN。" -> "When dynamic policy is enabled, it temporarily overrides the selection above: return NODATA first, then NXDOMAIN for frequent requests."
        text == "同一域名的所有记录类型合并计数" -> "Count all record types for the same domain together"
        text.contains(" 次 / ") && text.contains(" 秒 / 保持 ") -> text.replace(" 次 / ", " requests / ").replace(" 秒 / 保持 ", " seconds / hold ").replace(" 秒", " seconds")
        text == "正常客户端会缓存 NODATA 响应。动态策略主要用于持续重试或忽略负缓存的请求。" -> "Normal clients cache NODATA responses. Dynamic policy is mainly for repeated retries or requests that ignore negative caching."
        text == "关闭后每次解析都会请求上游 DNS" -> "When disabled, every resolution requests the upstream DNS"
        text == "已过期" -> "Expired"
        text.contains(" · ") && text.endsWith(" 后过期") -> text.replace(" 小时", " hours").replace(" 分钟", " minutes").replace(" 秒", " seconds").replace(" 后过期", " until expiration")
        text.endsWith(" 小时") -> text.removeSuffix(" 小时") + " hours"
        text.endsWith(" 分钟") -> text.removeSuffix(" 分钟") + " minutes"
        text.endsWith(" 秒") -> text.removeSuffix(" 秒") + " seconds"
        text == "请求次数" -> "Request count"
        text == "统计窗口（秒）" -> "Statistics window (seconds)"
        text == "NXDOMAIN 保持时长（秒）" -> "NXDOMAIN hold duration (seconds)"
        text == "记录与统计" -> "Records and statistics"
        text == "请求记录" -> "Request records"
        text == "查看 DNS 与 HTTPS 请求和处理结果" -> "View DNS and HTTPS requests and processing results"
        text == "缓存记录" -> "Cache records"
        text == "查看、搜索或清理已缓存的 DNS 结果" -> "View, search, or clear cached DNS results"
        text == "竞速解析" -> "Race resolution"
        text == "查看服务商的响应速度和成功情况" -> "View provider response speeds and success rates"
        text == "Bootstrap 解析" -> "Bootstrap resolution"
        text == "查看 DNS 服务地址的解析情况" -> "View resolution results for DNS service addresses"
        text == "规则拦截" -> "Rule blocking"
        text == "维护工具" -> "Maintenance tools"
        text == "黑名单规则" -> "Blocklist rules"
        text == "检查范围" -> "Inspection scope"
        text == "内置 DNS 服务商" -> "Built-in DNS providers"
        text == "协议与兼容性" -> "Protocols and compatibility"
        text == "删除本组全部订阅" -> "Delete all subscriptions in this group"
        text == "查看 HTTPS 检查根证书" -> "View HTTPS inspection root certificate"
        text == "填写镜像站名称和地址格式" -> "Enter the mirror name and URL format"
        text == "请输入域名或支持的白名单规则" -> "Enter a domain or supported allowlist rule"
        text == "权重按当前勾选的竞速服务商归一化为百分制，参与竞速的服务商合计为 100%。" -> "Weights are normalized to a 100-point scale using the currently selected race providers; participating providers total 100%."
        text == "查看各订阅拦截请求的次数和占比" -> "View blocked request counts and ratios for each subscription"
        text == "正在检查 GitHub Release" -> "Checking GitHub Releases"
        text == "检查 GitHub Release 中的最新版本" -> "Check GitHub Releases for the latest version"
        text.startsWith("发现 ") && text.contains(" 新版本，") -> text.replace(" 新版本，", " new version, ")
        text == "关闭搜索" -> "Close search"
        text == "安装和卸载 CA 证书方法" -> "How to install and remove CA certificates"
        text == "建议使用“标准”。档位会自动设置最长 TTL、最短 TTL 和解析失败兜底时间，不再需要手动填写秒数。" -> "Standard is recommended. Presets configure the maximum TTL, minimum TTL, and fallback time for failed resolutions, so seconds do not need to be entered manually."
        text == "运行数据" -> "Runtime data"
        text == "权重数据" -> "Weight data"
        text == "用户数据" -> "User data"
        text == "我知道了" -> "Got it"
        text == "动态参数" -> "Dynamic parameters"
        text == "返回域名不存在，并提供 5 分钟负缓存以减少重复查询" -> "Return NXDOMAIN with a five-minute negative cache to reduce repeated queries"
        text == "返回域名存在但没有该记录，并提供 5 分钟负缓存" -> "Return NODATA with a five-minute negative cache"
        text == "返回服务器拒绝该查询，不提供缓存记录" -> "Return REFUSED without a cacheable response"
        text == "A 返回 0.0.0.0，AAAA 返回 ::，其他类型返回空结果" -> "Return 0.0.0.0 for A, :: for AAAA, and an empty result for other types"
        text == "域名不存在（NXDOMAIN）" -> "Domain does not exist (NXDOMAIN)"
        text == "域名存在但无记录（NODATA）" -> "Domain exists but has no record (NODATA)"
        text == "拒绝查询（REFUSED）" -> "Query refused (REFUSED)"
        text == "返回零地址" -> "Return zero address"
        text == "重新验证" -> "Verify again"
        text == "尚未完成" -> "Not completed"
        text == "立即重置" -> "Reset now"
        text == "新增" -> "Add"
        text == "名称（可选）" -> "Name (optional)"
        text == "IP 地址" -> "IP address"
        text == "搜索应用或包名" -> "Search apps or package names"
        text.startsWith("收起") -> text.replace("收起", "Collapse ")
        text.startsWith("展开") -> text.replace("展开", "Expand ")
        text == "更新规则" -> "Update rules"
        text == "重命名订阅" -> "Rename subscription"
        text == "编辑订阅" -> "Edit subscription"
        text == "禁用规则" -> "Disable rules"
        text == "启用规则" -> "Enable rules"
        text == "删除规则" -> "Delete rules"
        text == "订阅信息" -> "Subscription information"
        text == "订阅分组" -> "Subscription group"
        text == "DNS 过滤规则" -> "DNS filtering rules"
        text == "网络 DNS 过滤订阅" -> "Network DNS filtering subscription"
        text == "hosts 覆写规则" -> "hosts override rules"
        text == "网络 hosts 覆写订阅" -> "Network hosts override subscription"
        text == "已导入 HTTPS" -> "Imported to HTTPS"
        text == "使用自定义镜像" -> "Use custom mirror"
        text == "选择镜像站模板" -> "Choose mirror template"
        text == "暂无模板，请先在域名规则 → 镜像站模板中添加。" -> "No templates. Add one under Domain rules -> Mirror templates first."
        text == "请求预览" -> "Request preview"
        text == "hosts 覆写" -> "hosts override"
        text == "DNS 过滤" -> "DNS filtering"
        text == " · 自定义镜像" -> " · Custom mirror"
        text == "本地文件" -> "Local file"
        text == "打开规则订阅操作" -> "Open rule subscription actions"
        text.startsWith("导入于 ") -> text.replace("导入于 ", "Imported on ")
        text.startsWith("更新于 ") -> text.replace("更新于 ", "Updated on ")
        text.startsWith("更新失败（连续 ") -> text.replace("更新失败（连续 ", "Update failed (").replace(" 次）：", " consecutive failures): ")
        text.startsWith("导入失败：") -> text.replace("导入失败：", "Import failed: ")
        text == "屏蔽规则" -> "Block rules"
        text == "放行规则" -> "Allow rules"
        text == "覆写域名规则" -> "Domain override rules"
        text == "覆写规则" -> "Override rules"
        text.startsWith("仅在 HTTPS 流量检查可解密的 HTTP(S) 请求中生效。") -> text.replace("仅在 HTTPS 流量检查可解密的 HTTP(S) 请求中生效。", "Applies only to HTTP(S) requests decrypted by HTTPS inspection. ").replace("域名规则 ", "Domain rules ").replace(" 条，URL 屏蔽/放行规则 ", ", URL block/allow rules ").replace(" 条，CNAME 覆写 ", ", CNAME overrides ").replace(" 条。", ".")
        text == "拦截策略" -> "Blocking policy"
        text == "拦截响应" -> "Block response"
        text == "动态策略：先 NODATA，高频请求后 NXDOMAIN" -> "Dynamic policy: NODATA first, then NXDOMAIN for frequent requests"
        text.startsWith("当前：") -> text.replace("当前：", "Current: ")
        text == "URL 请求规则" -> "URL request rules"
        text == "如 https://example.com/js，匹配该路径前缀" -> "For example, https://example.com/js; matches this path prefix"
        text == "添加放行 URL" -> "Add allowed URL"
        text == "如 https://example.com/allowed，可覆盖域名屏蔽" -> "For example, https://example.com/allowed; can override domain blocking"
        text == "URL 屏蔽规则" -> "URL block rules"
        text == "查看、启用、停用或删除 URL 屏蔽规则" -> "View, enable, disable, or delete URL block rules"
        text == "URL 放行规则" -> "URL allow rules"
        text == "查看、启用、停用或删除 URL 放行规则" -> "View, enable, disable, or delete URL allow rules"
        text == "域名屏蔽与放行" -> "Domain blocking and allowing"
        text == "输入要屏蔽的域名，如 example.com" -> "Enter a domain to block, e.g. example.com"
        text == "添加放行域名" -> "Add allowed domain"
        text == "输入要放行的域名，如 example.com" -> "Enter a domain to allow, e.g. example.com"
        text == "查看、启用、停用或删除屏蔽规则" -> "View, enable, disable, or delete block rules"
        text == "查看、启用、停用或删除放行规则" -> "View, enable, disable, or delete allow rules"
        text == "域名覆写" -> "Domain overrides"
        text == "添加覆写域名" -> "Add domain override"
        text == "将域名覆写为 IPv4 或 IPv6" -> "Override the domain with an IPv4 or IPv6 address"
        text == "将域名覆写为 CNAME" -> "Override the domain with a CNAME"
        text == "IPv4/IPv6 覆写规则" -> "IPv4/IPv6 override rules"
        text == "CNAME 覆写规则" -> "CNAME override rules"
        text == "查看、启用、停用或删除当前范围的覆写规则" -> "View, enable, disable, or delete overrides in the current scope"
        text == "订阅与更新" -> "Subscriptions and updates"
        text == "管理 DNS 域名规则与 IPv4/IPv6 覆写订阅" -> "Manage DNS domain-rule and IPv4/IPv6 override subscriptions"
        text == "管理 HTTPS 域名、URL 与 CNAME 覆写订阅" -> "Manage HTTPS domain, URL, and CNAME override subscriptions"
        text == "设置规则订阅的自动更新开关和频率" -> "Configure automatic update and frequency for rule subscriptions"
        text == "维护订阅下载镜像，可在添加订阅时直接选择" -> "Maintain subscription download mirrors and select them when adding subscriptions"
        text == "清理全部 DNS 规则" -> "Clear all DNS rules"
        text == "清理全部 HTTPS 规则" -> "Clear all HTTPS rules"
        text == "清除当前 DNS 规则后可重新导入，不影响 Go 隧道规则" -> "Clear current DNS rules for re-import; Go tunnel rules are not affected"
        text == "清除当前 HTTPS 规则后可重新导入，不影响传统 DNS 规则" -> "Clear current HTTPS rules for re-import; legacy DNS rules are not affected"
        text == "删除全部 HTTPS 规则" -> "Delete all HTTPS rules"
        text == "确定要删除全部 DNS 规则吗？手动添加和订阅导入的域名屏蔽、白名单及 IPv4/IPv6 覆写规则都会被移除。Go 隧道规则不受影响。" -> "Delete all DNS rules? Manually added and subscription-imported domain block, allowlist, and IPv4/IPv6 override rules will be removed. Go tunnel rules are not affected."
        text == "确定要删除全部 HTTPS 规则吗？手动添加和订阅导入的域名、URL、白名单和覆写规则都会被移除。传统 DNS 规则不受影响。" -> "Delete all HTTPS rules? Manually added and subscription-imported domain, URL, allowlist, and override rules will be removed. Legacy DNS rules are not affected."
        text == "证书状态" -> "Certificate status"
        text == "正在验证系统凭据库中的证书状态" -> "Verifying the certificate status in the system credential store"
        text == "用于所选应用的 HTTPS 流量检查" -> "Used for HTTPS inspection of selected apps"
        text == "已验证" -> "Verified"
        text == "未验证" -> "Not verified"
        text == "仅已安装并通过验证的根证书可用于 HTTPS 流量检查；部分应用可能因证书固定或自定义校验而不受支持。" -> "Only installed and verified root certificates can be used for HTTPS inspection; some apps may be unsupported due to certificate pinning or custom validation."
        text == "安装与查看" -> "Install and inspect"
        text == "安装和卸载 HTTPS 检查根证书" -> "Install or remove the HTTPS inspection root certificate"
        text == "查看 Android 系统 CA 证书的安装、卸载和安全说明" -> "View Android system CA certificate installation, removal, and security notes"
        text == "安装 HTTPS 检查根证书" -> "Install HTTPS inspection root certificate"
        text == "导出谛听 HTTPS 检查根证书，并前往系统设置完成安装" -> "Export the Ting HTTPS inspection root certificate and finish installation in system settings"
        text == "已安装" -> "Installed"
        text == "未安装" -> "Not installed"
        text == "查看当前 HTTPS 检查根证书的 SHA-256 指纹" -> "View the current HTTPS inspection root certificate SHA-256 fingerprint"
        text == "证书管理" -> "Certificate management"
        text == "废止当前证书并生成新证书；之后需要重新安装" -> "Revoke the current certificate and create a new one; it must then be installed again"
        text.startsWith("导出 HTTPS 检查根证书失败：") -> text.replace("导出 HTTPS 检查根证书失败：", "Failed to export HTTPS inspection root certificate: ")
        text == "HTTPS 检查根证书已重置" -> "HTTPS inspection root certificate reset"
        text == "重置 HTTPS 检查根证书失败" -> "Failed to reset HTTPS inspection root certificate"
        text.startsWith("请在系统设置中选择“从存储设备安装 CA 证书”") -> {
            val fileName = text.substringAfter("中的 ").substringBefore("。返回后")
            "In system settings, choose “Install CA certificate from storage”, then select $fileName in Downloads. Return here and Ting will verify installation through the system CA store using the SHA-256 fingerprint. If the system restricts access to the credential store, the status remains unverified.\n\nSuccessful installation only means the certificate was added to the system user credential store; it does not mean every app will trust it. Connections incompatible with certificate pinning, custom validation, or mutual TLS are bypassed by HTTPS inspection and recorded as “HTTPS inspection automatic bypass”."
        }
        text == "设置配置" -> "Configuration transfer"
        text == "配置文件仅包含所选的自定义配置，不包含日志、缓存和已下载的域名规则。" -> "The configuration file contains only selected custom settings, excluding logs, cache, and downloaded domain rules."
        text == "导出内容" -> "Export contents"
        text == "自定义 DNS 服务商" -> "Custom DNS providers"
        text == "名称、协议和解析地址" -> "Names, protocols, and resolution addresses"
        text == "自定义 Bootstrap IP" -> "Custom Bootstrap IPs"
        text == "名称、IP 和启用状态" -> "Names, IPs, and enabled states"
        text == "网络规则订阅" -> "Network rule subscriptions"
        text == "订阅名称和链接" -> "Subscription names and URLs"
        text == "排除应用" -> "Excluded apps"
        text == "使用系统 DNS 的应用包名" -> "Package names of apps using system DNS"
        text == "服务运行时禁止联网的应用包名" -> "Package names of apps blocked from networking while the service runs"
        text == "受限应用、独立域名白名单与启用状态" -> "Restricted apps, standalone domain allowlists, and enabled state"
        text == "配置文件" -> "Configuration file"
        text == "导出配置" -> "Export configuration"
        text == "将勾选内容保存为 JSON 配置文件" -> "Save selected items as a JSON configuration file"
        text == "导入配置" -> "Import configuration"
        text == "合并配置并跳过本机已有项目；网络订阅不会立即下载" -> "Merge the configuration and skip items already on this device; network subscriptions are not downloaded immediately"
        text.startsWith("已处理 ") -> text.replace("已处理 ", "Processed ")
        text == "正在准备" -> "Preparing"
        text == "Bootstrap DNS 解析统计" -> "Bootstrap DNS resolution statistics"
        text == "时间范围" -> "Time range"
        text == "统计 Bootstrap DNS 解析 DoH/DoT 服务商域名的使用情况。" -> "Statistics for DoH/DoT provider-domain usage resolved through Bootstrap DNS."
        text == "统计" -> "Statistics"
        text == "产生真实 DNS 查询或 DNS 查询测速后，这里会显示 Bootstrap DNS 的成功率和权重。" -> "Bootstrap DNS success rates and weights appear here after real DNS queries or DNS speed tests are recorded."
        text == "概览" -> "Overview"
        text == "Bootstrap DNS 使用排行" -> "Bootstrap DNS usage ranking"
        text == "整体表现" -> "Overall performance"
        text.startsWith("成功 ") && text.contains(" · 平均 ") -> text.replace("成功 ", "Success ").replace(" · 平均 ", " · Average ").replace(" · 备用 ", " · Fallback ").replace(" 次", " times")
        text.startsWith(" · 冷却中") -> text.replace("冷却中", "cooldown")
        text.startsWith(" · 连续失败 ") -> text.replace(" · 连续失败 ", " · Consecutive failures ")
        text.startsWith("暂无样本") -> "No samples"
        text == "Bootstrap 设置" -> "Bootstrap settings"
        text == "全局开关" -> "Global switch"
        text == "启用 Bootstrap IP" -> "Enable Bootstrap IP"
        text == "使用独立递归 DNS 解析 DoH/DoT 服务商域名，失败时自动尝试备用 IP" -> "Use independent recursive DNS to resolve DoH/DoT provider domains and try backup IPs on failure"
        text == "关闭后 DoH 使用系统 DNS，DoT 直接连接服务商域名。" -> "When disabled, DoH uses system DNS and DoT connects directly to the provider domain."
        text == "内置 Bootstrap IP" -> "Built-in Bootstrap IPs"
        text == "自定义 Bootstrap IP" -> "Custom Bootstrap IPs"
        text == "暂无自定义 Bootstrap IP。点击右上角“新增”添加。" -> "No custom Bootstrap IPs. Tap “Add” above to add one."
        text == "应用白名单访问" -> "App allowlist access"
        text == "所选应用只能连接其白名单域名解析出的 IP。直连 IP、局域网、加密 DNS 和其他连接会被阻止；共享 CDN IP 在 DNS TTL 内可能承载其他站点。" -> "Selected apps can connect only to IPs resolved from their allowlisted domains. Direct IPs, LAN, encrypted DNS, and other connections are blocked; a shared CDN IP may serve other sites during its DNS TTL."
        text == "启用应用白名单访问" -> "Enable app allowlist access"
        text == "请先添加至少一个白名单域名" -> "Add at least one allowlisted domain first"
        text.startsWith("已选择 ") && text.contains(" 个应用，") -> text.replace("已选择 ", "").replace(" 个应用，", " apps, ").replace(" 个域名", " domains")
        text == "选择受限应用" -> "Select restricted apps"
        text == "这些应用仅可访问白名单域名" -> "These apps can access only allowlisted domains"
        text == "白名单域名" -> "Allowlisted domains"
        text == "域名，如 example.com" -> "Domain, e.g. example.com"
        text == "请输入有效域名" -> "Enter a valid domain"
        text == "需先安装并验证 HTTPS 检查根证书" -> "Install and verify the HTTPS inspection root certificate first"
        text == "首次开启时默认选择全部用户应用" -> "The first enable selects all user apps by default"
        text == "尚未选择应用；开启后仅保留 DNS 过滤" -> "No apps selected; enabling this keeps DNS filtering only"
        text.startsWith("仅检查已选择的 ") -> text.replace("仅检查已选择的 ", "Inspecting only the selected ").replace(" 个应用；其他应用直接转发", " apps; other apps are forwarded directly")
        text == "没有可用于 HTTPS 检查的用户应用" -> "No user apps are available for HTTPS inspection"
        text == "无法初始化 HTTPS 检查应用" -> "Unable to initialize HTTPS inspection apps"
        text == "选择需要检查 HTTP(S) 流量的应用" -> "Choose apps whose HTTP(S) traffic should be inspected"
        text.startsWith("根证书已验证；") -> text.replace("根证书已验证；", "Root certificate verified; ").replace("可查看、重新安装或重置", "view, reinstall, or reset it")
        text == "安装并验证 HTTPS 检查根证书" -> "Install and verify the HTTPS inspection root certificate"
        text == "添加域名" -> "Add domain"
        text == "包含所有子域名" -> "Includes all subdomains"
        text == "保存后会自动从禁止联网应用、HTTPS 流量检查和排除 VPN 应用中移除。" -> "After saving, these apps are removed from blocked apps, HTTPS inspection, and VPN exclusions."
        text == "禁止联网应用" -> "Blocked apps"
        text == "通过本机 VPN 按 UID 阻止所选应用的全部网络连接。共享同一 UID 的应用会一并受影响。" -> "Block all network connections for selected apps by UID through the local VPN. Apps sharing the same UID are affected together."
        text == "此功能需要 Android 10 或更高版本，当前设备不满足运行条件，因此无法启用。" -> "This feature requires Android 10 or later; this device does not meet the requirements and cannot enable it."
        text == "启用禁止联网" -> "Enable network blocking"
        text == "尚未选择应用；开启后不会阻断流量" -> "No apps selected; traffic will not be blocked when enabled"
        text == "选择禁止联网应用" -> "Select blocked apps"
        text == "选择需要阻止联网的应用" -> "Select apps whose network access should be blocked"
        text == "关闭后名单会保留，但不会阻断流量或启用 Go 隧道。" -> "After disabling, the list is retained but traffic is not blocked and the Go tunnel is not enabled."
        text == "DNS 解析、域名规则和其他基础功能不会受到影响。" -> "DNS resolution, domain rules, and other core features are not affected."
        text == "服务开启时，所选应用的全部网络连接将被阻止。共享同一 UID 的应用会一并受影响。" -> "When the service is enabled, all network connections for selected apps are blocked. Apps sharing the same UID are affected together."
        text == "应用列表访问" -> "App list access"
        text == "继续" -> "Continue"
        text == "暂不允许" -> "Not now"
        text == "允许访问" -> "Allow access"
        text == "全选" -> "Select all"
        text == "反选" -> "Invert selection"
        text == "过滤" -> "Filter"
        text == "排序" -> "Sort"
        text == "删除壁纸" -> "Delete wallpaper"
        text == "确定删除这张已添加的壁纸吗？" -> "Delete this added wallpaper?"
        text == "取消" -> "Cancel"
        text == "删除" -> "Delete"
        text == "确定" -> "OK"
        text == "添加" -> "Add"
        text == "DNS 服务开启时" -> "When DNS service is enabled"
        text == "DNS 服务关闭时" -> "When DNS service is disabled"
        text == "暂无请求日志" -> "No request logs"
        text == "暂无有效缓存" -> "No valid cache entries"
        text == "暂无竞速数据" -> "No race data"
        text == "暂无 Bootstrap 数据" -> "No Bootstrap data"
        text == "暂无规则拦截数据" -> "No rule blocking data"
        text == "暂无拦截或错误记录" -> "No blocked or error records"
        text == "暂无订阅拦截数据" -> "No subscription blocking data"
        text == "DNS 缓存" -> "DNS cache"
        text == "请求日志" -> "Request logs"
        text == "搜索请求" -> "Search requests"
        text == "正在加载…" -> "Loading..."
        text == "筛选请求状态" -> "Filter request status"
        text == "处理域名" -> "Manage domain"
        text == "加入白名单规则" -> "Add to allowlist rules"
        text == "加入屏蔽规则" -> "Add to blocklist rules"
        text == "正在导入" -> "Importing"
        text == "本地 DNS 缓存" -> "Local DNS cache"
        text == "删除请求日志" -> "Delete request logs"
        text == "删除 DNS 缓存" -> "Delete DNS cache"
        text == "恢复 DNS 默认权重" -> "Restore default DNS weights"
        text == "恢复 Bootstrap 权重" -> "Restore Bootstrap weights"
        text == "删除全部 DNS 规则" -> "Delete all DNS rules"
        text == "删除全部 HTTPS 过滤规则" -> "Delete all HTTPS filtering rules"
        text == "重置所有新手引导" -> "Reset all onboarding guides"
        text == "CA证书设置" -> "CA certificate settings"
        text == "安装和卸载CA证书方法" -> "How to install and remove CA certificates"
        text == "设置配置" -> "Configuration settings"
        text == "规则导入" -> "Rule import"
        text == "导出配置" -> "Export configuration"
        text == "导入配置" -> "Import configuration"
        text == "知道了" -> "Got it"
        text == "清除" -> "Clear"
        text == "搜索缓存域名..." -> "Search cached domains..."
        text == "订阅地址" -> "Subscription URL"
        text == "删除规则订阅" -> "Delete rule subscription"
        text == "解散分组" -> "Dissolve group"
        text == "删除分组订阅" -> "Delete group subscriptions"
        text == "解散" -> "Dissolve"
        text == "添加屏蔽域名" -> "Add blocked domain"
        text == "添加放行域名" -> "Add allowed domain"
        text == "添加覆写域名" -> "Add domain override"
        text == "添加放行 URL" -> "Add allowed URL"
        text == "添加屏蔽 URL" -> "Add blocked URL"
        text == "要屏蔽的域名，如 example.com" -> "Domain to block, e.g. example.com"
        text == "要放行的域名，如 example.com" -> "Domain to allow, e.g. example.com"
        text == "请输入域名或支持的过滤规则" -> "Enter a domain or supported filter rule"
        text == "请输入要屏蔽的域名" -> "Enter a domain to block"
        text == "请输入要放行的域名" -> "Enter a domain to allow"
        text == "URL 规则无效、已存在，或包含不支持的通配符/修饰符" -> "The URL rule is invalid, already exists, or contains unsupported wildcards/modifiers"
        text == "域名、目标格式无效、规则冲突或已存在" -> "The domain or target format is invalid, conflicts with another rule, or already exists"
        text == "添加失败" -> "Add failed"
        text == "更新失败" -> "Update failed"
        text == "域名，如 example.com" -> "Domain, e.g. example.com"
        text == "完整 URL，如 https://example.com/js" -> "Full URL, e.g. https://example.com/js"
        text == "按协议、主机和路径前缀匹配，忽略查询参数" -> "Matches protocol, host, and path prefix; query parameters are ignored"
        text == "跳转到页面" -> "Go to page"
        text == "页码" -> "Page number"
        text == "跳转" -> "Go"
        text == "筛选规则来源" -> "Filter rule source"
        text.startsWith("请输入 1 到 ") && text.endsWith(" 之间的页码") -> text
            .replace("请输入 1 到 ", "Enter a page from ")
            .replace(" 之间的页码", "")
        text.startsWith("共 ") && text.contains(" 条") -> text
            .replace("共 ", "")
            .replace(" 条", " ")
            .replace("域名规则", "domain rules")
            .replace("白名单规则", "allowlist rules")
            .replace("屏蔽规则", "blocklist rules")
            .replace("覆写规则", "override rules")
        text == "搜索 URL 规则" -> "Search URL rules"
        text == "搜索域名或规则" -> "Search domains or rules"
        text == "上一页" -> "Previous page"
        text == "下一页" -> "Next page"
        text == "全部规则" -> "All rules"
        text == "手动添加" -> "Manually added"
        text == "已选中" -> "Selected"
        text == "当前模式" -> "Current mode"
        text == "内置服务协议" -> "Built-in service protocol"
        text == "模式配置" -> "Mode configuration"
        text == "查询服务" -> "Query provider"
        text == "候选服务" -> "Candidate providers"
        text == "同时查询的服务" -> "Providers queried in parallel"
        text == "依次尝试的服务" -> "Providers tried in order"
        text == "查询顺序" -> "Query order"
        text == "提高优先级" -> "Move up"
        text == "降低优先级" -> "Move down"
        text == "主" -> "Primary"
        text.startsWith("备 ") -> text.replace("备 ", "Backup ")
        text == "本地规则" -> "Local rules"
        text == "未配置" -> "Not configured"
        text.endsWith(" 个服务商") -> text.removeSuffix(" 个服务商") + " providers"
        text == "查看 Go 隧道限制" -> "View Go tunnel limitations"
        text.startsWith("Go 隧道用于在本机 VPN") -> "The Go tunnel takes over TCP, UDP, DNS, and HTTP(S) traffic in the local VPN so that Ting can identify app connections.\n\nTriggered features are active.\n\nWhile enabled, smart selection and fastest-response modes are unavailable; use single-provider or primary-backup mode instead."
        text.startsWith("仅切换阿里云和 DNSPod 内置服务") -> "Switch only the DNS, DoT, or DoH protocol of the built-in Alibaba Cloud and DNSPod services, and sync the corresponding preset providers in all four modes"
        text.startsWith("仅影响阿里云和 DNSPod") -> "Only built-in Alibaba Cloud and DNSPod services are affected; the corresponding preset providers selected in all four resolution modes will switch protocols together."
        text.startsWith("测速域名") -> "Test domain"
        text.startsWith("会向已选择的测速服务商") -> "The selected test providers query this domain three consecutive times each, and the average of successful samples is used."
        text.startsWith("可选择 1 个或多个服务商") -> "Select one or more providers. This selection is only used for DNS query latency tests and does not affect race mode."
        text.startsWith("测速结果按成功优先") -> "Results are sorted by success first and then by average latency; they only reflect the current network state."
        text == "长按并拖动调整顺序" -> "Long-press and drag to reorder"
        text == "选择一个 DNS 服务商进行查询" -> "Choose one DNS provider for queries"
        text == "根据近期成功率和延迟优先选择服务，失败或超时时自动兜底" -> "Prioritize providers by recent success rate and latency, with automatic fallback on failure or timeout"
        text == "同时查询所有选中服务，采用最先成功的结果" -> "Query all selected providers in parallel and use the first successful result"
        text == "按设置顺序查询，前一个失败后切换下一个服务" -> "Query providers in order and switch to the next after a failure"
        text == "选择一个服务进行查询。此选择与首页当前 DNS 服务商保持一致。" -> "Choose one provider for queries. This matches the current DNS provider on the home screen."
        text.startsWith("已选择 ") && text.contains(" 个候选服务。") -> text.replace("已选择 ", "").replace(" 个候选服务。至少选择 2 个；系统会根据近期成功率和延迟优先选择，并在失败或超时时自动兜底。", " candidate providers selected. Select at least 2; providers are prioritized by recent success rate and latency, with automatic fallback on failure or timeout.")
        text.startsWith("已选择 ") && text.contains(" 个同时查询的服务。") -> text.replace("已选择 ", "").replace(" 个同时查询的服务。至少选择 2 个；查询会同时发送并采用最先成功的结果。", " providers selected for parallel queries. Select at least 2; queries are sent simultaneously and the first success is used.")
        text.startsWith("已选择 ") && text.contains(" 个依次尝试的服务。") -> text.replace("已选择 ", "").replace(" 个依次尝试的服务。至少需要 1 个主服务和 1 个备用服务；长按并拖动可调整查询顺序。", " providers selected for ordered fallback. Select at least one primary and one backup; long-press and drag to reorder.")
        text == "测速域名" -> "Test domain"
        text == "用于测速的域名" -> "Domain used for testing"
        text == "测速服务商" -> "Test providers"
        text == "查询耗时结果" -> "Query latency results"
        text == "测速中..." -> "Testing..."
        text == "测试查询耗时" -> "Test query latency"
        text.startsWith("暂无 ") && text.endsWith(" DNS 服务商。") -> text.replace("暂无 ", "No ").replace(" DNS 服务商。", " DNS providers.")
        text.startsWith("平均 ") && text.contains(" 次成功") -> text.replace("平均 ", "Average ").replace(" 次成功", " successful")
            .replace(" · 样本 ", " · Samples ")
        text.startsWith("全部失败（") -> text.replace("全部失败（", "All failed (").replace(" 次）", " attempts)")
        text == "服务商健康" -> "Provider health"
        text == "DNS 提供商健康情况" -> "DNS provider health"
        text.startsWith("权重按当前勾选") -> "Weights are normalized to percentages among selected race providers; participating providers total 100%."
        text == "刷新" -> "Refresh"
        text.endsWith("\n暂无运行数据") -> text.removeSuffix("\n暂无运行数据") + "\nNo runtime data"
        text.startsWith("准确率 ") -> text.replace("准确率 ", "Accuracy ").replace(" · 延迟 ", " · Latency ").replace(" · 样本 ", " · Samples ").replace(" · 更新 ", " · Updated ")
        text.startsWith("权重 ") -> text.replace("权重 ", "Weight ")
        text == "未参与" -> "Not participating"
        text == "打开最近请求" -> "Open recent requests"
        text == "悬浮窗日志启动失败" -> "Failed to start floating log window"
        text == "最近请求" -> "Recent requests"
        text == "最小化悬浮窗日志" -> "Minimize floating log window"
        text.startsWith("调整悬浮窗大小，当前") -> text.replace("调整悬浮窗大小，当前", "Resize floating log window; current size: ")
        text == "小" -> "Small"
        text == "中" -> "Medium"
        text == "大" -> "Large"
        text == "关闭悬浮窗日志" -> "Close floating log window"
        text.startsWith("悬浮窗：") -> text.replace("悬浮窗：", "Floating window: ")
        text == "小" -> "Small"
        text == "中" -> "Medium"
        text == "大" -> "Large"
        text == "正在加载日志…" -> "Loading logs..."
        text == "拦截" -> "Blocked"
        text == "全部" -> "All"
        text == "放行" -> "Allowed"
        text == "直连" -> "Direct"
        text == "已放行" -> "Allowed"
        text == "已覆写" -> "Rewritten"
        text == "已拦截" -> "Blocked"
        text == "无效请求" -> "Invalid request"
        text == "HTTPS 检查自动旁路" -> "HTTPS inspection bypass"
        text == "协议不支持 · 直连" -> "Unsupported protocol · Direct"
        text == "资源保护 · 直连" -> "Resource protection · Direct"
        text.startsWith("当前筛选下暂无请求记录") -> "No request records for the current filter"
        text.startsWith("“已放行/已拦截”表示") -> "“Allowed/blocked” means DNSSR read the HTTP request authority and matched it against the current domain rules.\n\n“HTTPS inspection bypass” means HTTPS inspection forwarded the connection directly because of certificate pinning, mutual TLS, EV certificates, security-domain policies, or a handshake failure; the HTTP request inside was not read.\n\nRequest records store only the app, authority, protocol, result, matched rule, and time; paths, headers, and bodies are not stored."
        text == "错误" -> "Error"
        text.contains("命中缓存") -> text.replace("命中缓存", "Cache hit")
        text == "重写" -> "Rewritten"
        text == "绕过" -> "Bypassed"
        text == "未取得请求地址" -> "Request address unavailable"
        text == "导入规则" -> "Import rules"
        text == "订阅名称" -> "Subscription name"
        text == "自定义更新时间" -> "Custom update interval"
        text == "小时" -> "Hours"
        text == "暂无分组" -> "No groups"
        text == "Go 隧道已启用" -> "Go tunnel enabled"
        text == "选择内置服务协议" -> "Choose built-in service protocol"
        text == "选择解析模式" -> "Choose resolution mode"
        text == "HTTPS 请求记录" -> "HTTPS request records"
        text == "请求记录说明" -> "Request record details"
        text == "本地订阅导入后无法更新。" -> "Local subscriptions cannot be updated after import."
        text == "确认并继续" -> "Confirm and continue"
        text == "解析设置" -> "Resolution"
        text == "性能优化" -> "Performance"
        text == "运行行为" -> "Behavior"
        text == "外观" -> "Appearance"
        text == "数据管理" -> "Data management"
        text == "服务商管理" -> "Provider management"
        text == "查询测速" -> "Speed test"
        text == "缓存设置" -> "Cache settings"
        text == "解析模式" -> "Resolution mode"
        text == "日志模式" -> "Log mode"
        text == "前后台行为" -> "Foreground and background behavior"
        text == "排除应用" -> "Excluded apps"
        text == "导入与导出" -> "Import and export"
        text == "数据清理" -> "Data cleanup"
        text == "域名规则使用说明" -> "Domain rules guide"
        text == "外观设置" -> "Appearance settings"
        text == "出站代理" -> "Outbound proxy"
        text == "拦截响应" -> "Blocking response"
        text == "固定响应" -> "Fixed response"
        text == "动态策略" -> "Dynamic policy"
        text == "启用动态策略" -> "Enable dynamic policy"
        text == "后台" -> "Background"
        text == "后台隐藏" -> "Hide in background"
        text == "开启后隐藏最近任务卡片，并在支持的系统上禁用任务截图" -> "Hide the recent-task card and disable task screenshots on supported systems"
        text == "通知常驻" -> "Persistent notification"
        text == "VPN 未运行时在通知栏常驻提醒，不影响 VPN 运行中的连接通知" -> "Keep a reminder in the notification shade while the VPN is stopped; this does not affect connection notifications while it runs"
        text == "忽略电池优化" -> "Ignore battery optimizations"
        text == "当前 Android 版本不支持电池优化设置" -> "This Android version does not support battery optimization settings"
        text.startsWith("SHA-256 指纹：") -> text.replace("SHA-256 指纹：", "SHA-256 fingerprint:")
        text.startsWith("正在读取 HTTPS 检查根证书的 SHA-256 指纹") -> "Reading the HTTPS inspection root certificate SHA-256 fingerprint..."
        text.startsWith("重置会删除当前 HTTPS 检查私有 CA") -> "Resetting deletes the current private HTTPS inspection CA and creates a new private key and root certificate. Existing system certificates are not removed automatically. Remove the old certificate from system credential settings, then install and verify the new certificate before resuming HTTPS inspection in compatible apps."
        text.startsWith("确定删除“") -> text.replace("确定删除“", "Delete “").replace("”吗？删除后无法再作为解析服务使用。", "”? It can no longer be used for DNS resolution.")
        text.startsWith("当前用于解析 DNS：") -> text.replace("当前用于解析 DNS：", "Currently used for DNS: ")
        text.contains(" 解析地址") -> text.replace(" 解析地址", " resolution URL")
        text.contains(" 服务器地址") -> text.replace(" 服务器地址", " server address")
        text.contains(" 端口") -> text.replace(" 端口", " port")
        text == "暂无请求记录" -> "No request records"
        text == "当前的地址并不符合要求" -> "The current address is invalid"
        text.startsWith("匹配规则 · ") -> text.replace("匹配规则 · ", "Matched rule · ")
        text == "通过" -> "Passed"
        text == "全部" -> "All"
        text.startsWith("仅已安装并通过验证的根证书") -> "Only an installed and verified root certificate can be used for HTTPS traffic inspection. Some apps may not be supported because of certificate pinning or custom validation."
        text == "电池" -> "Battery"
        text == "界面显示" -> "Interface"
        text == "日夜模式" -> "Light and dark mode"
        text == "选择应用使用的浅色或深色外观" -> "Choose the light or dark appearance used by the app"
        text == "主题色配置" -> "Accent color"
        text == "选择应用界面的强调色" -> "Choose the accent color used by the app interface"
        text == "首页透明度" -> "Home-screen transparency"
        text == "分别调整首页按钮、选择框与文字的透明度" -> "Adjust the transparency of home buttons, selectors, and text"
        text == "首页内容" -> "Home content"
        text == "首页句子" -> "Home messages"
        text == "分别设置 DNS 服务开启和关闭时的句子" -> "Set separate messages for when the DNS service is enabled and disabled"
        text == "通知栏文案" -> "Notification text"
        text == "分别设置 DNS 服务开启和关闭时的通知栏文案" -> "Set separate notification text for when the DNS service is enabled and disabled"
        text == "Go 隧道关闭按钮" -> "Go tunnel close button"
        text == "Go 隧道未启用时隐藏首页关闭按钮" -> "Hide the home close button when the Go tunnel is not enabled"
        text == "背景与动效" -> "Background and animation"
        text == "软件背景" -> "App background"
        text == "选取手机图片作为应用背景" -> "Choose a photo on the phone as the app background"
        text == "设置服务启动和关闭时的动态光影效果" -> "Configure animation when the service starts and stops"
        text == "兼容功能" -> "Compatibility"
        text == "旧版图标" -> "Legacy icon"
        text == "切换桌面入口图标的样式" -> "Change the style of the launcher icon"
        text == "旧版日志页面" -> "Legacy log page"
        text == "选择首页日志按钮打开的页面" -> "Choose the page opened by the home Logs button"
        text == "使用旧版图标" -> "Use legacy icon"
        text == "开启后仅将桌面入口图标切换为旧版样式" -> "Only the launcher icon changes to the legacy style"
        text == "使用旧版日志页面" -> "Use legacy log page"
        text == "开启后首页日志按钮显示当前的日志分组页；关闭后显示新版日志仪表盘" -> "Show the current log groups from the home Logs button; when disabled, show the new log dashboard"
        text == "Go 隧道未启用时隐藏" -> "Hide when the Go tunnel is not enabled"
        text == "关闭后，首页始终显示 Go 隧道关闭按钮" -> "When disabled, always show the Go tunnel close button on the home screen"
        text == "启用服务动态光影" -> "Enable service animation"
        text == "自定义背景" -> "Custom background"
        text == "启用软件背景" -> "Enable app background"
        text == "添加图片" -> "Add image"
        text == "已添加壁纸" -> "Added wallpapers"
        text == "交互组件" -> "Interaction components"
        text == "文字" -> "Text"
        text == "主题色" -> "Accent color"
        text == "主题" -> "Theme"
        text == "系统会在后台按此频率检查网络规则订阅，实际执行时间可能受系统调度影响。" -> "The system checks rule subscriptions in the background at this interval. The actual execution time may be affected by system scheduling."
        text == "仅会自动更新已开启的分组中的网络订阅。" -> "Only subscriptions in enabled groups are updated automatically."
        text.startsWith("暂无 ") && text.contains(" 自定义服务商。点击右上角") -> text.replace("暂无 ", "No custom ").replace(" 自定义服务商。点击右上角“新增”添加自己的 DNS 服务。", " providers. Tap Add above to add your own DNS service.")
        text == "删除 DNS 服务商" -> "Delete DNS provider"
        text == "服务商名称" -> "Provider name"
        text == "选择、添加或编辑 DoH/DoT 服务" -> "Choose, add, or edit DoH/DoT services"
        text == "配置全局 Bootstrap DNS 与智慧权重" -> "Configure global Bootstrap DNS and smart weights"
        text == "缓存已解析的域名，减少重复查询" -> "Cache resolved domains to reduce repeated queries"
        text == "选择单一服务、智能选择、最快响应或依次尝试策略" -> "Choose single-provider, smart selection, fastest response, or primary-backup mode"
        text == "选择 DNS 请求日志的记录范围" -> "Choose what DNS request logs record"
        text == "后台隐藏、通知常驻和电池设置" -> "Background hiding, persistent notifications, and battery settings"
        text == "指定使用系统 DNS 的应用" -> "Specify apps that use the system DNS"
        text == "备份或恢复自定义服务与规则订阅" -> "Back up or restore custom services and rule subscriptions"
        text == "选择配置内容并导入或导出" -> "Choose configuration content to import or export"
        text == "将当前生效规则导出为 TXT 文件" -> "Export active rules as a TXT file"
        text == "从本地文件导入 DNS、hosts 和 HTTPS 规则" -> "Import DNS, hosts, and HTTPS rules from a local file"
        text == "删除缓存、日志或域名规则" -> "Delete cache, logs, or domain rules"
        text == "将过滤后的流量转发到本地 SOCKS5 或 HTTP 代理" -> "Forward filtered traffic to a local SOCKS5 or HTTP proxy"
        text == "选择应用界面语言" -> "Choose the app interface language"
        text.startsWith("应用设置 · ") -> text.replace("应用设置 · ", "App settings · ")
        text == "选择提供本地代理端口的应用。代理应用本身会绕过 DNSSR。" -> "Choose the app that provides the local proxy port. The proxy app itself bypasses DNSSR."
        text == "启用出站代理功能" -> "Enable outbound proxy"
        text == "代理应用" -> "Proxy app"
        text == "选择代理应用" -> "Choose proxy app"
        text == "自动更新设置" -> "Automatic update settings"
        text == "自动更新" -> "Automatic updates"
        text == "自动更新规则订阅" -> "Automatically update rule subscriptions"
        text == "在后台定期更新所有网络订阅，实际执行时间可能受系统调度影响" -> "Periodically update all network subscriptions in the background; actual execution may be affected by system scheduling"
        text == "自动更新时间" -> "Automatic update interval"
        text == "分组自动更新" -> "Group automatic updates"
        text == "自定义间隔" -> "Custom interval"
        text.startsWith("每 ") && text.endsWith(" 小时") -> text.removePrefix("每 ").removeSuffix(" 小时") + " hours"
        text == "输入 1 至 168 小时之间的更新时间" -> "Enter an update interval between 1 and 168 hours"
        text == "已连接" -> "Connected"
        text == "谛听" -> "DNSSR"
        text == "已开启" -> "On"
        text == "已关闭" -> "Off"
        text.startsWith("已切换为") -> text.replace("已切换为", "Resolution mode changed to ")
            .replace("单一服务", "Single provider")
            .replace("智能选择", "Smart selection")
            .replace("最快响应", "Fastest response")
            .replace("依次尝试", "Primary and backup")
        text.startsWith("Resolution mode changed to ") -> text
            .replace("单一服务", "Single provider")
            .replace("智能选择", "Smart selection")
            .replace("最快响应", "Fastest response")
            .replace("依次尝试", "Primary and backup")
        text == "谛听 服务" -> "DNSSR service"
        text == "谛听 监控" -> "DNSSR monitor"
        text == "谛听万象，明察清浊" -> "The readiness is all."
        text == "收耳静眠，归于无声" -> "The rest is silence."
        text == "连接失败" -> "Connection failed"
        text == "正在连接出站代理" -> "Connecting to outbound proxy"
        text.startsWith("已连接 · ") -> text.replace("已连接 · ", "Connected · ")
            .replace(" 个服务商）", " providers)")
            .replace("（", "(")
            .replace("单一服务", "Single provider")
            .replace("智能选择", "Smart selection")
            .replace("最快响应", "Fastest response")
            .replace("依次尝试", "Primary and backup")
            .replace("自定义", "Custom")
        text.startsWith("出站代理不可用 · ") -> text.replace("出站代理不可用 · ", "Outbound proxy unavailable · ")
        text.startsWith("已选择 ") && text.contains(" 个应用，") && text.endsWith(" 个域名") -> {
            text.removePrefix("已选择 ").replace(" 个应用，", " apps selected, ").removeSuffix(" 个域名") + " domains"
        }
        text.startsWith("连续失败 ") -> text.replace("连续失败 ", "Consecutive failures: ")
        text.startsWith("成功 ") && text.contains("%") -> text.replace("成功 ", "Success ").replace(" · 平均 ", " · Average ").replace(" · 备用 ", " · Fallback ").replace(" 次", " times")
        text.startsWith(" · 延迟 ") -> text.replace(" · 延迟 ", " · Latency ").replace(" ms", " ms")
        text.endsWith(" 已完成") -> text.removeSuffix(" 已完成") + " completed"
        text.endsWith(" 失败") -> text.removeSuffix(" 失败") + " failed"
        text.startsWith("服务开启时，所选应用的全部网络连接将被阻止") -> "When the service is enabled, all network connections from selected apps are blocked. Apps sharing the same UID are affected as well."
        text.startsWith("通过本机 VPN 按 UID 阻止") -> "Block all network connections from selected apps by UID through the local VPN. Apps sharing the same UID are affected as well."
        text.startsWith("此功能需要 Android 10 或更高版本") -> "This feature requires Android 10 or later."
        text.startsWith("关闭后名单会保留") -> "The list is retained when disabled, but traffic is not blocked and the Go tunnel is not enabled."
        text.startsWith("DNS 解析、域名规则") -> "DNS resolution, domain rules, and other basic features are not affected."
        text.startsWith("建议使用“标准”") -> "Balanced is recommended. The preset automatically configures maximum TTL, minimum TTL, and the fallback duration for failed resolution, so you do not need to enter seconds manually."
        text.startsWith("关闭后 DoH 使用系统 DNS") -> "When disabled, DoH uses the system DNS and DoT connects directly to the provider domain."
        text.startsWith("暂无自定义 Bootstrap IP") -> "No custom Bootstrap IPs. Tap Add above to create one."
        text.startsWith("统计 Bootstrap DNS 解析") -> "Track Bootstrap DNS resolution usage for DoH/DoT provider domains."
        text.startsWith("产生真实 DNS 查询") -> "Bootstrap DNS success rates and weights appear here after real DNS queries or DNS latency tests."
        text.startsWith("动态策略开启时会临时覆盖") -> "When dynamic policy is enabled, it temporarily overrides the selection: it returns NODATA first, then NXDOMAIN for frequent requests."
        text.startsWith("正常客户端会缓存 NODATA") -> "Normal clients cache NODATA responses. Dynamic policy is mainly for repeated retries or requests that ignore negative caching."
        text.startsWith("同一域名的所有记录类型合并计数") -> "Combine all record types for the same domain when counting requests."
        text.startsWith("请求次数需在 ") -> text.replace("请求次数需在 ", "Request count must be between ").replace("之间", ".")
        text.startsWith("统计窗口需在 ") -> text.replace("统计窗口需在 ", "The statistics window must be between ").replace(" 秒之间", " seconds.")
        text.startsWith("保持时长需在 ") -> text.replace("保持时长需在 ", "The hold duration must be between ").replace(" 秒之间", " seconds.")
        text.startsWith("桌面图标可能需要等待") -> "The launcher may need time to refresh the desktop icon. If it does not change immediately, return to the desktop and wait a moment."
        text.startsWith("软件背景与服务动态光影不可同时启用") -> "The app background and service animation cannot be enabled at the same time."
        text.startsWith("两项内容均可留空") -> text.replace("两项内容均可留空；留空后", "Both fields may be left blank; when blank,").replace("首页不显示句子", "no home-screen status sentence is shown").replace("对应通知栏会使用默认状态文案", "the notification uses the default status text")
        text.startsWith("关闭“通知常驻”") -> "Disabling persistent notifications only stops monitoring reminders while the VPN is not running. The required foreground-service notification remains visible while the VPN is running."
        text.startsWith("需要系统悬浮窗权限") -> "System overlay permission is required. The floating window hides automatically when the app returns to the foreground."
        text.startsWith("超过所选时间的 DNS 请求日志") -> "DNS request logs older than the selected period are deleted automatically to control local storage usage."
        text == "为了让你选择需要排除或进行 HTTP(S) 检查的应用，谛听需要读取设备上的应用列表。不会读取应用数据，也不会上传应用列表。" -> "DNSSR needs access to the installed app list so you can choose apps to exclude or inspect for HTTP(S) traffic. It does not read app data or upload the app list."
        text == "地址" -> "Address"
        text == "端口" -> "Port"
        text == "账号（可选）" -> "Username (optional)"
        text == "密码（可选）" -> "Password (optional)"
        text == "添加域名" -> "Add domain"
        text == "包含所有子域名" -> "Includes all subdomains"
        text == "模板名称" -> "Template name"
        text == "模板地址" -> "Template URL"
        text == "删除镜像站模板" -> "Delete mirror template"
        text == "Bootstrap DNS 解析统计" -> "Bootstrap DNS resolution statistics"
        text == "整体表现" -> "Overall performance"
        text == "服务动态光影" -> "Service animation"
        text == "HTTPS 检查根证书" -> "HTTPS inspection root certificate"
        text == "验证 HTTPS 检查根证书" -> "Verify HTTPS inspection root certificate"
        text == "重置 HTTPS 检查根证书" -> "Reset HTTPS inspection root certificate"
        text == "新增 Bootstrap IP" -> "Add Bootstrap IP"
        text == "当前筛选下暂无请求日志" -> "No request logs for the current filter"
        text == "复制域名" -> "Copy domain"
        text.startsWith("软件介绍\n") -> "App overview\nDNSSR is a DNS management tool based on Android's local VPN. It supports DNS, DoH, DoT, provider management, caching, rules, logs, and per-app controls.\n\nImportant notes\nThe app relies on the local VPN and upstream DNS services. Resolution speed, stability, and availability depend on your network, selected upstream, and rule configuration. Caches, logs, rules, and settings are stored on this device, but upstream services still receive the queries required for resolution. Extended features such as HTTPS traffic inspection may affect network behavior; use them carefully after understanding their purpose.\n\nDisclaimer\nThis software is provided as-is, without guarantees of availability, speed, or compatibility with every device and network. Verify the reliability of upstream services, rules, and configuration sources, and accept the associated risks. Do not use this software for illegal purposes; you are responsible for any network, data, or other losses caused by its configuration or use."
        text.startsWith("通知用于显示 DNS VPN") -> "Notifications show the running and stopped state of the DNS VPN. Denying this permission will not block core features, but you may not receive timely connection status updates."
        text.startsWith("谛听需要建立本地 VPN") -> "DNSSR needs a local VPN to process and filter DNS requests. This permission lets the app handle DNS traffic on the device; it does not send all network traffic to a remote VPN server."
        text.startsWith("正在检查 GitHub Release") -> "Checking GitHub Release"
        text == "检查失败，点击重试" -> "Check failed. Tap to retry"
        text.startsWith("检查 GitHub Release 中") -> "Checking GitHub Release for the latest version"
        text == "可下载" -> "Available to download"
        text == "安装包已下载，点击安装" -> "Package downloaded. Tap to install"
        text == "上次下载失败，点击重试" -> "Previous download failed. Tap to retry"
        text.startsWith("当前版本 ") -> text.replace("当前版本 ", "Current version ")
        text == "下载中" -> "Downloading"
        text == "安装" -> "Install"
        text == "重新下载" -> "Download again"
        text == "下载更新" -> "Download update"
        text.startsWith("检查更新失败：") -> text.replace("检查更新失败：", "Update check failed: ")
        text.startsWith("下载更新失败：") -> text.replace("下载更新失败：", "Update download failed: ")
        text == "最新版本未提供 Android APK" -> "The latest version does not provide an Android APK"
        text == "更新包下载地址无效" -> "The update package URL is invalid"
        text.startsWith("已下载 ") -> text.replace("已下载 ", "Downloaded ")
        text == "更新与支持" -> "Updates and support"
        text == "当前版本" -> "Current version"
        text == "检查更新" -> "Check for updates"
        text == "发现新版本" -> "New version available"
        text == "关闭启动时检查更新" -> "Disable update checks at startup"
        text == "应用启动时不自动检查，可随时手动检查" -> "Do not check automatically at startup; check manually at any time"
        text == "应用启动时自动检查新版本" -> "Automatically check for new versions at startup"
        text == "加入 QQ 群" -> "Join QQ group"
        text == "稍后" -> "Later"
        text.startsWith("交流群：") -> text.replace("交流群：", "Group: ").replace("（入群答案：", " (join answer: ").replace("）", ")")
        text == "时间范围" -> "Time range"
        text == "统计" -> "Statistics"
        text == "正在加载" -> "Loading"
        text == "概览" -> "Overview"
        text == "Bootstrap DNS 使用排行" -> "Bootstrap DNS usage ranking"
        text.startsWith("整体表现") -> "Overall performance"
        text.startsWith("成功 ") && text.contains(" · 平均 ") && text.contains(" · 备用 ") -> text.replace("成功 ", "Success ").replace(" · 平均 ", " · Average ").replace(" · 备用 ", " · Fallback ").replace(" 次", " times")
        text.endsWith(" 次") -> text.removeSuffix(" 次") + " times"
        text.contains(" · 冷却中") -> text.replace(" · 冷却中", " · Cooling down").replace(" · 连续失败 ", " · Consecutive failures: ")
        text.contains(" · 权重 ") -> text.replace("成功 ", "Success ").replace(" · 平均 ", " · Average ").replace(" · 权重 ", " · Weight ")
        text == "Bootstrap 设置" -> "Bootstrap settings"
        text == "全局开关" -> "Global switch"
        text == "启用 Bootstrap IP" -> "Enable Bootstrap IP"
        text.startsWith("使用独立递归 DNS") -> "Use independent recursive DNS to resolve DoH/DoT provider domains, with automatic fallback to backup IPs on failure"
        text == "内置 Bootstrap IP" -> "Built-in Bootstrap IPs"
        text == "自定义 Bootstrap IP" -> "Custom Bootstrap IPs"
        text == "暂无样本" -> "No samples"
        text == "冷却中" -> "Cooling down"
        text.startsWith(" · 延迟 ") -> text.replace(" · 延迟 ", " · Latency ")
        text.startsWith("感谢为谛听提出建议") -> "Thanks for suggestions and testing help"
        text == "共建者名单" -> "Co-builders"
        text == "头像均已缓存，无需刷新" -> "All avatars are cached; no refresh needed"
        text.startsWith("已刷新 ") -> text.replace("已刷新 ", "Refreshed ").replace(" 个头像", " avatars").replace("，", ", ").replace("仍未加载", "still not loaded")
        text == "刷新头像" -> "Refresh avatars"
        text.startsWith("感谢每一位为谛听提出建议") -> "Thanks to every co-builder who contributed suggestions and testing help. The list is sorted alphabetically by username, with Chinese names sorted by pinyin."
        text == "暂时还没有共建者，期待在这里写下你的名字。" -> "There are no co-builders yet. We look forward to adding your name here."
        text == "配置" -> "Configuration"
        text.startsWith("备份或恢复自定义服务") -> "Back up or restore custom providers and rule subscriptions"
        text == "规则" -> "Rules"
        text == "将当前生效规则导出为可订阅的 TXT 文件" -> "Export active rules as a subscribable TXT file"
        text == "配置已导出" -> "Configuration exported"
        text.startsWith("导入完成：新增 ") -> text.replace("导入完成：新增 ", "Import complete: added ").replace(" 项，跳过 ", ", skipped ").replace(" 项，失败 ", ", failed ").replace(" 项", " items")
        text == "无法打开导出文件" -> "Unable to open export file"
        text == "正在准备导出" -> "Preparing export"
        text == "正在写入文件" -> "Writing file"
        text == "正在读取配置文件" -> "Reading configuration file"
        text == "正在读取 HTTPS 规则" -> "Reading HTTPS rules"
        text == "正在生成 HTTPS 备份" -> "Generating HTTPS backup"
        text == "正在读取白名单规则" -> "Reading allowlist rules"
        text == "正在读取拦截规则" -> "Reading block rules"
        text == "正在生成导出文件" -> "Generating export file"
        text == "无法读取配置文件" -> "Unable to read configuration file"
        text.startsWith("排除应用：") -> text.replace("排除应用：", "Excluded app: ")
        text.startsWith("禁止联网应用：") -> text.replace("禁止联网应用：", "Blocked app: ")
        text.startsWith("应用白名单访问：") -> text.replace("应用白名单访问：", "App allowlist access: ")
        text.startsWith("应用白名单域名：") -> text.replace("应用白名单域名：", "App allowlist domain: ")
        text.startsWith("操作失败：") -> text.replace("操作失败：", "Operation failed: ").replace("未知错误", "Unknown error")
        text == "配置文件不是有效的 JSON" -> "The configuration file is not valid JSON"
        text.startsWith("DNS ") && text.contains("已导出：") -> text.replace("DNS ", "DNS ").replace("订阅规则已导出：", "subscription rules exported: ").replace("手动添加规则已导出：", "manually added rules exported: ").replace("全部规则已导出：", "all rules exported: ")
        text.startsWith("HTTPS ") && text.contains("已导出：") -> text.replace("HTTPS ", "HTTPS ").replace("订阅规则已导出：", "subscription rules exported: ").replace("手动添加规则已导出：", "manually added rules exported: ").replace("全部规则已导出：", "all rules exported: ")
        text.startsWith("屏蔽 ") && text.contains(" 条，放行 ") -> text.replace("屏蔽 ", "Blocked ").replace(" 条，放行 ", ", allowed ").replace(" 条，CNAME ", ", CNAME ").replace("，URL 屏蔽 ", ", URL blocked ").replace(" 条，URL 放行 ", ", URL allowed ").replace(" 条", " rules")
        text == "不支持的配置文件版本" -> "The configuration file version is not supported"
        text == "配置中包含不支持的 DNS 协议" -> "The configuration contains an unsupported DNS protocol"
        text == "配置中包含无效的 DNS 服务商" -> "The configuration contains an invalid DNS provider"
        text == "配置中包含无效的 Bootstrap IP" -> "The configuration contains an invalid Bootstrap IP"
        text == "配置中包含无效的订阅链接" -> "The configuration contains an invalid subscription URL"
        text.startsWith("配置中包含不支持的订阅作用域：") -> text.replace("配置中包含不支持的订阅作用域：", "The configuration contains an unsupported subscription scope: ")
        text.startsWith("配置缺少 ") -> text.replace("配置缺少 ", "The configuration is missing ")
        text.startsWith("配置字段 ") && text.endsWith(" 格式错误") -> text.removeSuffix(" 格式错误").removePrefix("配置字段 ") + " has an invalid format"
        text == "配置列表格式错误" -> "The configuration list has an invalid format"
        text == "规则格式无效或规则已存在" -> "The rule format is invalid or the rule already exists"
        text == "恢复完成：" -> "Restore complete:"
        text.startsWith("恢复完成：屏蔽 ") -> text.replace("恢复完成：屏蔽 ", "Restore complete: blocked ").replace(" 条，放行 ", ", allowed ").replace(" 条，CNAME ", ", CNAME ").replace(" 条，URL 屏蔽 ", ", URL blocked ").replace(" 条，URL 放行 ", ", URL allowed ").replace(" 条，跳过 ", ", skipped ").replace(" 条", " rules")
        text == "HTTPS 规则备份不是有效的 JSON" -> "The HTTPS rule backup is not valid JSON"
        text == "不是 HTTPS 规则备份文件" -> "This is not an HTTPS rule backup file"
        text == "不支持的 HTTPS 规则备份版本" -> "The HTTPS rule backup version is not supported"
        text == "备份包含不支持的规则来源" -> "The backup contains an unsupported rule source"
        text == "备份不属于 HTTPS 规则" -> "The backup does not belong to HTTPS rules"
        text == "备份缺少规则内容" -> "The backup is missing rule contents"
        text.startsWith("备份包含无效的") -> text.replace("备份包含无效的", "The backup contains an invalid ").replace("屏蔽规则", "block rule").replace("放行规则", "allow rule").replace("覆写规则", "override rule").replace("URL 规则", "URL rule")
        text == "备份包含不支持的覆写规则" -> "The backup contains an unsupported override rule"
        text.startsWith("备份缺少 ") -> text.replace("备份缺少 ", "The backup is missing ")
        text == "备份规则格式错误" -> "The backup rule has an invalid format"
        text == "配置中包含无效的 URL 规则" -> "The backup contains an invalid URL rule"
        text.startsWith("首次导入因应用意外终止而中断") -> "The initial import was interrupted because the app stopped unexpectedly. Residual rules were cleared; import again."
        text.startsWith("更新因应用意外终止而中断") -> "The update was interrupted because the app stopped unexpectedly. Existing rules were kept; update again."
        text == "应用信息" -> "App information"
        text == "谛听 / RESOLUTION UNDER YOUR CONTROL" -> "DNSSR / RESOLUTION UNDER YOUR CONTROL"
        text == "一位集美大学人工智能系大三学子" -> "A junior AI student at Jimei University"
        text == "开源项目仓库" -> "Open-source repository"
        text == "打开项目仓库" -> "Open project repository"
        text == "核心能力" -> "Core capabilities"
        text == "运行边界" -> "Operating boundaries"
        text == "加密上游" -> "Encrypted upstreams"
        text == "规则与缓存" -> "Rules and cache"
        text == "可观测性" -> "Observability"
        text == "独立引导" -> "Independent bootstrap"
        text == "配置流转" -> "Configuration transfer"
        text == "快捷控制" -> "Quick controls"
        text == "应用管控" -> "App controls"
        text == "HTTPS 流量检查" -> "HTTPS traffic inspection"
        text == "支持 DNS、DoH 与 DoT，并可管理自定义服务商。" -> "Supports DNS, DoH, and DoT, with custom provider management."
        text == "以缓存、屏蔽规则、白名单和订阅规则减少重复请求与干扰。" -> "Reduces repeated requests and interference with caching, block rules, allowlists, and subscription rules."
        text == "通过请求日志、竞速统计、服务商健康和 Bootstrap 数据追踪状态。" -> "Track status through request logs, race statistics, provider health, and Bootstrap data."
        text == "支持 Bootstrap IP，降低解析加密上游域名时对系统 DNS 的依赖。" -> "Supports Bootstrap IPs to reduce reliance on system DNS when resolving encrypted upstream domains."
        text == "支持自定义服务、规则订阅的导入导出与自动更新。" -> "Supports importing, exporting, and automatically updating custom providers and rule subscriptions."
        text == "提供系统快捷设置磁贴，方便快速启停 DNS 本地通道。" -> "Provides a system Quick Settings tile for rapidly starting and stopping the local DNS channel."
        text == "按应用排除 VPN 或阻止联网，灵活控制每个应用的网络访问。" -> "Exclude apps from the VPN or block their network access for per-app control."
        text == "按应用检查 HTTP(S) 请求并应用域名和 URL 规则，需安装 HTTPS 检查根证书。" -> "Inspect HTTP(S) requests per app and apply domain and URL rules; an HTTPS inspection root certificate is required."
        text == "默认仅处理 DNS" -> "DNS only by default"
        text.startsWith("默认通过 Android VpnService") -> "By default, Android VpnService creates a local channel that handles only DNS. When HTTPS inspection or network blocking is enabled, the Go tunnel takes over TCP, UDP, DNS, and HTTP(S) traffic using the Go userspace network stack."
        text == "实际解析表现" -> "Resolution behavior"
        text == "解析速度、稳定性和可用性取决于网络环境、所选上游和本机规则。" -> "Resolution speed, stability, and availability depend on the network, selected upstreams, and local rules."
        text == "隐私与本地存储" -> "Privacy and local storage"
        text == "缓存、日志、规则与配置保存在设备本机。上游仍会收到必要查询；仅 DoH、DoT 加密到上游的 DNS 传输。" -> "Caches, logs, rules, and settings are stored on this device. Upstreams still receive necessary queries; only DoH and DoT encrypt DNS transport to upstreams."
        text.startsWith("版本 ") -> text.replace("版本 ", "Version ")
        text == "本地优先" -> "Local first"
        text == "谛听 本地解析控制台" -> "Ting local resolution console"
        text == "在 Android 设备本机管理 DNS 查询路径，让解析策略、缓存、规则与上游连接处于可控状态。" -> "Manage the DNS path on the Android device and keep resolution policies, cache, rules, and upstream connections under control."
        text == "无法打开链接" -> "Unable to open link"
        text == "CA 文件已保存，请在系统设置中手动安装" -> "CA file saved. Install it manually in system settings."
        text.startsWith("已删除订阅 #") -> text.replace("已删除订阅 #", "Deleted subscription #")
        text == "已保存检查应用" -> "Inspection apps saved"
        text == "未授予悬浮窗权限，悬浮窗日志未开启" -> "Overlay permission was not granted; floating logs are disabled"
        text == "无法打开悬浮窗权限设置" -> "Unable to open overlay permission settings"
        text == "关闭后不会删除已有日志，重新开启即可继续查看。" -> "Disabling this does not delete existing logs; enable it again to continue viewing them."
        text == "暂无模板。点击上方“添加模板”开始使用。" -> "No templates. Tap Add template above to get started."
        text == "添加模板" -> "Add template"
        text.startsWith("确定要删除“") -> text.replace("确定要删除“", "Delete “").replace("”吗？已使用此模板的订阅不会被修改。", "”? Subscriptions already using this template will not be changed.")
        text == "例如：GitHub 镜像" -> "Example: GitHub mirror"
        text == "失败后回退直连" -> "Fall back to direct connection after failure"
        text == "解析服务" -> "Resolution provider"
        text == "解析服务（" -> "Resolution provider ("
        text == "选择解析服务" -> "Choose resolution provider"
        text == "更新所有订阅" -> "Update all subscriptions"
        text == "加载失败" -> "Loading failed"
        text == "旁路" -> "Bypassed"
        text == "覆写" -> "Rewritten"
        text == "未取得 authority" -> "Authority unavailable"
        text.contains(" 后过期") -> text.replace(" 后过期", " until expiry")
        text == "规则订阅" -> "Rule subscriptions"
        text == "正在导入中" -> "Import already in progress"
        text == "导入失败" -> "Import failed"
        text == "添加订阅失败" -> "Failed to add subscription"
        text == "切换订阅状态失败" -> "Failed to change subscription state"
        text.startsWith("镜像下载失败") -> text.replace("镜像下载失败", "Mirror download failed")
        text == "订阅链接必须使用 HTTP 或 HTTPS" -> "Subscription URL must use HTTP or HTTPS"
        text == "该订阅链接已存在" -> "This subscription URL already exists"
        text == "订阅名称不能为空" -> "Subscription name cannot be empty"
        text == "该文件已作为订阅导入" -> "This file has already been imported as a subscription"
        text == "订阅不存在" -> "Subscription not found"
        text == "订阅不属于当前规则范围" -> "The subscription is outside the current rule scope"
        text == "本地文件订阅仅支持重命名" -> "Local file subscriptions only support renaming"
        text == "本地文件订阅无法更新" -> "Local file subscriptions cannot be updated"
        text == "更新已取消，已保留原有规则" -> "Update canceled; existing rules were kept"
        text == "订阅响应为空" -> "Subscription response is empty"
        text == "订阅中没有可导入的有效 DNS 规则" -> "The subscription contains no valid importable DNS rules"
        text == "订阅中没有可导入的有效 hosts 规则" -> "The subscription contains no valid importable hosts rules"
        text == "原始订阅地址格式无效" -> "The original subscription URL is invalid"
        text == "镜像模板必须包含 {url}、{urlEncoded}、{scheme}、{host}、{path} 或 {pathAndQuery}" -> "The mirror template must contain {url}, {urlEncoded}, {scheme}, {host}, {path}, or {pathAndQuery}"
        text == "镜像模板生成的地址必须使用 HTTP 或 HTTPS" -> "The URL generated by the mirror template must use HTTP or HTTPS"
        text == "规则订阅自动更新" -> "Automatic rule subscription updates"
        text == "规则订阅自动更新失败" -> "Automatic rule subscription update failed"
        text == "规则订阅自动更新完成" -> "Automatic rule subscription update completed"
        text == "规则订阅已自动更新" -> "Rule subscriptions updated automatically"
        text.startsWith("第 ") && text.contains(" 个") -> text.replace("第 ", "").replace(" 个", "").replace("，正在下载", ", downloading").let { "Item $it" }
        text == "正在自动更新规则订阅" -> "Automatically updating rule subscriptions"
        text == "正在重试自动更新规则订阅" -> "Retrying automatic rule subscription updates"
        text.startsWith("正在准备") -> text.replace("正在准备", "Preparing").replace("，正在下载", ", downloading")
        text.endsWith("，正在下载") -> text.removeSuffix("，正在下载") + ", downloading"
        text.startsWith("成功 ") && text.contains("，失败 ") && text.contains("；更新 ") && !text.contains("，共导入 ") -> text.replace("成功 ", "Success ").replace(" 个，失败 ", ", failed ").replace(" 个；更新 ", "; updated ").replace(" 个，无需更新 ", ", unchanged ").replace(" 个", " items")
        text.startsWith("成功 ") && text.contains("，失败 ") && text.contains("，共导入 ") -> text.replace("成功 ", "Success ").replace(" 个，失败 ", ", failed ").replace(" 个；更新 ", "; updated ").replace(" 个，无需更新 ", ", unchanged ").replace(" 个，共导入 ", ", imported ").replace(" 条规则", " rules")
        text.startsWith("已重命名规则订阅") -> "Rule subscription renamed"
        text.startsWith("重命名失败：") -> text.replace("重命名失败：", "Rename failed: ")
        text == "已创建分组" -> "Group created"
        text.startsWith("创建分组失败：") -> text.replace("创建分组失败：", "Failed to create group: ")
        text == "分组名称不能为空" -> "Group name cannot be empty"
        text == "分组名称已存在" -> "Group name already exists"
        text.contains("：黑名单 ") && text.contains("，白名单 ") && text.contains("，覆写 ") && text.contains("，重复 ") && text.contains("，无效/不支持 ") -> text
            .replace("导入成功：", "Import successful: ")
            .replace("更新成功：", "Update successful: ")
            .replace("订阅已保存：", "Subscription saved: ")
            .replace("导入完成：", "Import complete: ")
            .replace("：黑名单 ", ": blocklist ")
            .replace(" 条，白名单 ", " items, allowlist ")
            .replace(" 条，覆写 ", " items, overrides ")
            .replace(" 条，重复 ", " items, duplicates ")
            .replace(" 条，无效/不支持 ", " items, invalid/unsupported ")
            .replace(" 条", " items")
        text == "正在启用规则订阅..." -> "Enabling rule subscription..."
        text == "正在禁用规则订阅..." -> "Disabling rule subscription..."
        text == "已启用规则订阅" -> "Rule subscription enabled"
        text == "已禁用规则订阅" -> "Rule subscription disabled"
        text.startsWith("切换失败：") -> text.replace("切换失败：", "Switch failed: ")
        text == "正在更新所有规则订阅..." -> "Updating all rule subscriptions..."
        text.startsWith("操作失败：") -> text.replace("操作失败：", "Operation failed: ")
        text == "添加失败" -> "Add failed"
        text == "更新失败" -> "Update failed"
        text == "操作失败" -> "Operation failed"
        text == "本次更新暂未提供详细说明。" -> "No detailed release notes are available for this update."
        text == "目标域名" -> "Target domain"
        text.endsWith(" 地址") -> text.removeSuffix(" 地址") + " address"
        text.startsWith("已添加到屏蔽规则") -> "Added to block rules"
        text.startsWith("已添加到白名单规则") -> "Added to allowlist rules"
        text.startsWith("已添加 URL 规则") -> "URL rule added"
        text.startsWith("已添加覆写域名") -> "Override domain added"
        text == "镜像站名称不能为空" -> "Mirror name cannot be empty"
        text == "模板必须使用 HTTP 或 HTTPS" -> "Template must use HTTP or HTTPS"
        text == "模板缺少 URL 占位符" -> "Template is missing a URL placeholder"
        text == "未知的规则操作" -> "Unknown rule operation"
        text == "导入成功" -> "Import successful"
        text == "订阅已保存" -> "Subscription saved"
        text == "更新成功" -> "Update successful"
        text.startsWith("更新成功，共导入 ") -> text.replace("更新成功，共导入 ", "Update successful; imported ").replace(" 条规则", " rules")
        text == "订阅已是最新" -> "Subscription is already up to date"
        text.startsWith("检查完成：") -> text.removePrefix("检查完成：").replace("更新", "updated ").replace("个，已是最新", ", up to date ").replace("个，失败", ", failed ").replace("个，共导入", ", imported ").replace("条规则", " rules")
        text == "导入完成" -> "Import complete"
        text.startsWith("hosts 导入完成：") -> text.replace("hosts 导入完成：新增 ", "hosts import complete: added ").replace(" 条，跳过 ", ", skipped ").replace(" 条", " rules")
        text == "HTTPS 规则备份只能在 HTTPS 过滤中导入" -> "HTTPS rule backups can only be imported in HTTPS filtering"
        text == "缺少规则文件" -> "Rule file is missing"
        text == "无法读取所选文件" -> "Unable to read the selected file"
        text == "文件中没有可导入的有效 DNS 规则" -> "The file contains no valid importable DNS rules"
        text == "文件中没有可导入的真实 IP hosts 规则" -> "The file contains no valid IP hosts rules"
        text == "自定义" -> "Custom"
        text == "当前已是最新版本。" -> "You are already using the latest version."
        text.startsWith("发现 ") && text.endsWith(" 新版本。") -> text.replace("发现 ", "New version available: ").replace(" 新版本。", ".")
        text == "检查更新失败。" -> "Update check failed."
        text == "未检测到 QQ，请搜索群号 1090225658 加入。" -> "QQ was not found. Search for group 1090225658 to join."
        text == "安装包不存在或无法打开，请重新下载。" -> "The package does not exist or cannot be opened. Download it again."
        text == "更新包下载失败，请重试。" -> "Update package download failed. Try again."
        text == "下载完成，请点击安装。" -> "Download complete. Tap Install."
        text == "无法开始下载更新。" -> "Unable to start the update download."
        text == "传统 DNS 过滤" -> "Traditional DNS filtering"
        text == "服务显示" -> "Provider visibility"
        text == "日夜模式" -> "Light and dark mode"
        text == "主题色配置" -> "Accent color"
        text == "首页透明度" -> "Home-screen opacity"
        text == "首页句子" -> "Home messages"
        text == "通知栏文案" -> "Notification text"
        text == "Go 隧道关闭按钮" -> "Go tunnel close button"
        text == "软件背景" -> "App background"
        text == "服务动态光影" -> "Service animation"
        text == "旧版图标" -> "Legacy icon"
        text == "旧版日志页面" -> "Legacy log page"
        text == "界面显示" -> "Interface"
        text == "首页内容" -> "Home content"
        text == "背景与动效" -> "Background and animation"
        text == "兼容功能" -> "Compatibility"
        text == "分别调整首页按钮、选择框与文字的透明度" -> "Adjust the opacity of home buttons, selectors, and text"
        text == "分别设置 DNS 服务开启和关闭时的句子" -> "Set separate messages for when the DNS service is enabled and disabled"
        text == "分别设置 DNS 服务开启和关闭时的通知栏文案" -> "Set separate notification text for when the DNS service is enabled and disabled"
        text == "Go 隧道未启用时隐藏首页关闭按钮" -> "Hide the home close button when the Go tunnel is disabled"
        text == "选取手机图片作为应用背景" -> "Choose a photo on the phone as the app background"
        text == "设置服务启动和关闭时的动态光影效果" -> "Configure animation when the service starts and stops"
        text == "切换桌面入口图标的样式" -> "Change the launcher icon style"
        text == "选择首页日志按钮打开的页面" -> "Choose the page opened by the home Logs button"
        text == "使用旧版图标" -> "Use legacy icon"
        text == "开启后仅将桌面入口图标切换为旧版样式" -> "Only switch the launcher icon to the legacy style"
        text == "使用旧版日志页面" -> "Use legacy log page"
        text == "开启后首页日志按钮显示当前的日志分组页；关闭后显示新版日志仪表盘" -> "Show the current log groups from the home Logs button; when disabled, show the new log dashboard"
        text == "Go 隧道未启用时隐藏" -> "Hide when the Go tunnel is disabled"
        text == "关闭后，首页始终显示 Go 隧道关闭按钮" -> "When disabled, always show the Go tunnel close button on the home screen"
        text == "启用服务动态光影" -> "Enable service animation"
        text == "软件背景已启用，服务动态光影不可同时使用" -> "The app background is enabled; service animation cannot be used at the same time"
        text == "启动和关闭服务时，光影从电源按钮向整个页面展开或收回" -> "When the service starts or stops, the light effect expands from the power button across the page or retracts"
        text == "需要 Android 13 或更高版本" -> "Android 13 or later is required"
        text == "光影效果代码来源于开源项目:" -> "The light-effect code comes from the open-source project:"
        text.startsWith("光影效果代码来源于开源项目:\n") -> "The light-effect code comes from the open-source project:\n" + text.substringAfter(":\n")
        text == "依据 AdGuard DNS 语法自动分类黑白名单。支持 BOM、行尾注释、hosts 多域名和 IDN 域名。" -> "Classify block and allow rules automatically using AdGuard DNS syntax. BOM, end-of-line comments, multiple hosts domains, and IDN domains are supported."
        text == "自定义背景" -> "Custom background"
        text == "请先添加一张图片" -> "Add an image first"
        text == "启用后服务动态光影将自动关闭" -> "Enabling this automatically disables service animation"
        text == "添加图片" -> "Add image"
        text == "已添加壁纸" -> "Added wallpapers"
        text == "开启软件背景" -> "Enable app background"
        text == "关闭软件背景" -> "Disable app background"
        text == "开启软件背景会关闭服务动态光影。是否继续？" -> "Enabling the app background will disable service animation. Continue?"
        text == "关闭软件背景后，是否开启服务动态光影？" -> "After disabling the app background, enable service animation?"
        text == "继续开启" -> "Keep enabled"
        text == "开启" -> "Enable"
        text == "不开启" -> "Do not enable"
        text == "软件背景" -> "App background"
        text == "当前背景" -> "Current background"
        text == "交互组件" -> "Interaction components"
        text == "启动按钮" -> "Start button"
        text == "解析服务选择框" -> "Resolution provider selector"
        text == "模式切换按钮" -> "Mode switch button"
        text == "文字" -> "Text"
        text == "首页古诗" -> "Home poem"
        text == "DNS 服务详情" -> "DNS service details"
        text == "主题" -> "Theme"
        text == "使用系统壁纸的动态取色" -> "Use dynamic colors from the system wallpaper"
        text == "请求日志仪表盘" -> "Request log dashboard"
        text == "全部 DNS、HTTPS、缓存、竞速与规则拦截的实时概览" -> "A real-time overview of DNS, HTTPS, cache, race, and rule blocking"
        text == "记录全部请求" -> "Record all requests"
        text == "仅记录拦截与错误" -> "Record blocked and error requests only"
        text == "请求日志已关闭" -> "Request logging is disabled"
        text == "无" -> "None"
        text == "功能中心" -> "Feature center"
        text == "关闭 Go 隧道" -> "Turn off Go tunnel"
        text == "镜像站模板" -> "Mirror templates"
        text == "保存常用的订阅下载镜像。添加规则订阅时，可以直接选择这里的模板。" -> "Save commonly used subscription download mirrors. Choose these templates directly when adding rule subscriptions."
        text == "操作" -> "Actions"
        text.startsWith("已保存模板（") -> text.replace("已保存模板（", "Saved templates (").replace("）", ")")
        text.endsWith(" 的更多操作") -> text.removeSuffix(" 的更多操作") + " more actions"
        text == "编辑模板" -> "Edit template"
        text == "删除模板" -> "Delete template"
        text == "镜像站格式示例" -> "Mirror format example"
        text == "了解占位符如何组成镜像地址" -> "Learn how placeholders form a mirror URL"
        text == "编辑镜像站模板" -> "Edit mirror template"
        text == "添加镜像站模板" -> "Add mirror template"
        text == "修改后，已使用此模板的订阅不会被自动更新。" -> "Subscriptions already using this template will not be updated automatically after changes."
        text == "保存后，可在添加规则订阅时直接选择。" -> "After saving, it can be selected directly when adding a rule subscription."
        text == "必须使用 HTTP(S) 并包含至少一个占位符" -> "Must use HTTP(S) and contain at least one placeholder"
        text == "已添加镜像站模板" -> "Mirror template added"
        text == "已更新镜像站模板" -> "Mirror template updated"
        text == "保存中..." -> "Saving..."
        text == "添加中..." -> "Adding..."
        text == "域名规则" -> "Domain rules"
        text == "规则" -> "Rules"
        text == "地址规则" -> "Address rules"
        text == "规则类型" -> "Rule types"
        text == "统一管理域名屏蔽、放行及 IPv4/IPv6 覆写，DNS 与 HTTPS 检查共用" -> "Manage domain blocking, allowing, and IPv4/IPv6 overrides shared by DNS and HTTPS inspection"
        text == "管理 HTTPS 解密后匹配的 URL 地址和路径前缀屏蔽、放行规则" -> "Manage URL address and path-prefix block and allow rules after HTTPS decryption"
        text == "规则范围" -> "Rule scope"
        text == "DNS 域名规则" -> "DNS domain rules"
        text == "HTTPS 域名规则" -> "HTTPS domain rules"
        text == "用于普通 DNS 请求的屏蔽、放行及 IPv4/IPv6 覆写" -> "Block, allow, and IPv4/IPv6 override rules for ordinary DNS requests"
        text == "仅在 HTTPS 流量检查可解密的请求中生效，包含屏蔽、放行及 CNAME 覆写" -> "Applies only to decryptable HTTPS inspection requests; includes block, allow, and CNAME override rules"
        text == "规则导出" -> "Rule export"
        text == "DNS 规则" -> "DNS rules"
        text == "HTTPS 规则" -> "HTTPS rules"
        text.startsWith("按域名规则和地址规则分别导出") -> "Export active domain and address rules separately. Domain rules use subscribable TXT; address rules use restorable JSON backups."
        text.startsWith("导出所有订阅导入的当前生效规则") -> "Export all active rules imported from subscriptions"
        text.startsWith("导出所有手动添加的当前生效规则") -> "Export all active manually added rules"
        text.startsWith("合并订阅导入与手动添加的当前生效规则") -> "Merge active rules imported from subscriptions and added manually"
        text.startsWith("导出") && text.contains("当前生效规则") -> text.replace("导出", "Export").replace("当前生效规则", "active rules")
        text == "配置文件仅包含所选的自定义配置，不包含日志、缓存和已下载的域名规则。" -> "The configuration file contains only selected custom settings; it excludes logs, cache, and downloaded domain rules."
        text == "导出内容" -> "Export contents"
        text == "名称、协议和解析地址" -> "Name, protocol, and resolution address"
        text == "名称、IP 和启用状态" -> "Name, IP, and enabled state"
        text == "订阅名称和链接" -> "Subscription name and URL"
        text == "使用系统 DNS 的应用包名" -> "Package names of apps using system DNS"
        text == "服务运行时禁止联网的应用包名" -> "Package names of apps blocked from networking while the service runs"
        text == "受限应用、独立域名白名单与启用状态" -> "Restricted apps, independent domain allowlists, and enabled state"
        text == "合并配置并跳过本机已有项目；网络订阅不会立即下载" -> "Merge configuration and skip existing local items; network subscriptions are not downloaded immediately"
        text.startsWith("已处理 ") -> text.replace("已处理 ", "Processed ").replace("正在准备", "Preparing")
        text == "竞速统计" -> "Race statistics"
        text == "缓存命中的 DNS 请求不会触发实际竞速，因此不计入这里的统计。" -> "Cache-hit DNS requests do not trigger an actual race and are not included in these statistics."
        text == "策略概览" -> "Strategy overview"
        text == "服务商胜出排行" -> "Provider win ranking"
        text == "暂无胜出记录" -> "No wins recorded"
        text == "智能选择首选" -> "Smart-selection primary wins"
        text == "暂无智能选择记录" -> "No smart-selection records"
        text == "智能选择产生查询后，会统计首选服务商的命中情况。" -> "Primary-provider wins are counted after smart selection generates queries."
        text.startsWith("成功 ") && text.contains(" · 首选命中 ") -> text.replace("成功 ", "Success ").replace(" · 平均 ", " · Average ").replace(" · 首选命中 ", " · Primary wins ").replace(" · 兜底 ", " · Fallback ").replace(" 次", " times")
        text.startsWith("平均胜出耗时 ") -> text.replace("平均胜出耗时 ", "Average win latency ")
        text.startsWith("首选命中 ") -> text.replace("首选命中 ", "Primary wins ").replace(" · 平均首选耗时 ", " · Average primary latency ")
        text == "订阅规则拦截率" -> "Subscription rule block rate"
        text == "拦截率为所选时间范围内，该订阅拦截请求数占全部已记录请求（DNS + HTTPS）的比例。" -> "The block rate is the share of all recorded requests (DNS + HTTPS) blocked by the subscription in the selected time range."
        text == "全部已记录请求" -> "All recorded requests"
        text == "正在加载统计数据" -> "Loading statistics"
        text == "DNS 与 HTTPS 日志合计，含通过、屏蔽和失败" -> "Combined DNS and HTTPS logs, including passed, blocked, and failed requests"
        text == "屏蔽订阅" -> "Blocking subscriptions"
        text == "暂无屏蔽订阅" -> "No blocking subscriptions"
        text == "导入屏蔽订阅并产生 DNS 或 HTTPS 请求后，此处将显示其拦截率。" -> "The block rate appears here after a blocking subscription is imported and generates DNS or HTTPS requests."
        text == "已删除" -> "Deleted"
        text == "已删除当前范围的全部规则" -> "All rules in the current scope were deleted"
        text == "已启用" -> "Enabled"
        text == "已禁用" -> "Disabled"
        text.contains(" | 拦截 ") -> text.replace(" | 拦截 ", " | Blocked ").replace(" 次 | ", " times | ")
        text == "应用列表菜单" -> "App list menu"
        text == "过滤应用" -> "Filter apps"
        text == "排序方式" -> "Sort order"
        text == "应用列表操作" -> "App list actions"
        text == "全部应用" -> "All apps"
        text == "系统应用" -> "System apps"
        text == "用户应用" -> "User apps"
        text == "已勾选应用" -> "Selected apps"
        text == "应用名称 A-Z" -> "App name A-Z"
        text == "应用名称 Z-A" -> "App name Z-A"
        text == "包名 A-Z" -> "Package name A-Z"
        text == "包名 Z-A" -> "Package name Z-A"
        text == "跟随系统" -> "Follow system"
        text == "中文" -> "Chinese"
        text == "记录全部" -> "Record all"
        text == "仅拦截和错误" -> "Blocked and errors only"
        text == "关闭" -> "Off"
        text == "浅色模式" -> "Light mode"
        text == "深色模式" -> "Dark mode"
        text == "单一服务" -> "Single provider"
        text == "智能选择" -> "Smart selection"
        text == "最快响应" -> "Fastest response"
        text == "依次尝试" -> "Primary and backup"
        text == "正在连接" -> "Connecting"
        text == "代理可用" -> "Proxy available"
        text == "代理不可用" -> "Proxy unavailable"
        text == "未运行" -> "Not running"
        text == "未选择" -> "Not selected"
        text == "已保存" -> "Saved"
        text.startsWith("已保存，") -> text.replace("已保存，DNS VPN 正在重连", "Saved; DNS VPN is reconnecting").replace("已保存，下次启动 DNS VPN 时生效", "Saved; it takes effect the next time DNS VPN starts")
        text == "日志已导出" -> "Logs exported"
        text == "导出失败" -> "Export failed"
        text == "已添加规则" -> "Rule added"
        text == "规则格式无效" -> "Invalid rule format"
        text == "以下操作会立即删除本机数据，删除后无法恢复。" -> "The following actions immediately delete local data and cannot be undone."
        text == "暂无 DNS 缓存" -> "No DNS cache entries"
        text.startsWith("原始 TTL ") -> text.replace("原始 TTL ", "Original TTL ").replace(" · 命中 ", " · Hits ").replace(" 次 · ", " times · ")
        text.startsWith("最后命中 ") -> text.replace("最后命中 ", "Last hit ").replace("无", "Never")
        text == "日志仪表盘" -> "Log dashboard"
        text == "查看全部" -> "View all"
        text == "详情" -> "Details"
        text == "隐私模式运行中" -> "Privacy mode is running"
        text == "重新开启日志后，仪表盘将从新产生的请求开始统计。" -> "After logging is enabled again, the dashboard will count new requests."
        text == "暂无规则订阅" -> "No rule subscriptions"
        text == "该分组暂无规则订阅" -> "This group has no rule subscriptions"
        text == "点击右上角 + 添加 AdGuard DNS 规则地址" -> "Tap + above to add an AdGuard DNS rule URL"
        text.startsWith("确定删除「") && !text.contains("中的全部") -> text.replace("确定删除「", "Delete “").replace("」及其导入的所有规则吗？", "” and all imported rules?")
        text.startsWith("解散「") -> text.replace("解散「", "Dissolve “").replace("」后，组内订阅将移至未分组，不会删除订阅及规则。", "”; subscriptions in the group will move to Ungrouped. Subscriptions and rules will not be deleted.")
        text.startsWith("确定删除「") && text.contains("中的全部") -> text.replace("确定删除「", "Delete all subscriptions and rules in “").replace("」中的全部", "” (").replace(" 个订阅及其规则吗？DNS ", " subscriptions and rules? DNS ").replace(" 个，HTTPS ", ", HTTPS ").replace(" 个。", ".")
        text.startsWith("上次尝试于 ") -> text.replace("上次尝试于 ", "Last attempted at ")
        text == "正在下载并更新规则..." -> "Downloading and updating rules..."
        text == "添加规则订阅" -> "Add rule subscription"
        text == "输入 AdGuard DNS 规则订阅地址，导入时会自动区分黑名单和白名单规则。" -> "Enter an AdGuard DNS rule subscription URL. Blocklist and allowlist rules are separated automatically during import."
        text == "复制 DNS 订阅导入" -> "Copy DNS subscription import"
        text == "复制 DNS 的网络过滤订阅到 HTTPS，之后两边可独立维护。" -> "Copy a DNS network-filtering subscription to HTTPS so they can be maintained independently."
        text == "暂无可导入的 DNS 网络过滤订阅" -> "No importable DNS network-filtering subscriptions"
        text == "编辑规则订阅" -> "Edit rule subscription"
        text == "重命名分组" -> "Rename group"
        text == "分组名称" -> "Group name"
        text == "新建分组名称（可选）" -> "New group name (optional)"
        text == "订阅名称（可选）" -> "Subscription name (optional)"
        text == "未分组" -> "Ungrouped"
        text == "未使用" -> "Not used"
        text == "未选择模板" -> "No template selected"
        text == "自定义镜像" -> "Custom mirror"
        text == "重命名规则订阅" -> "Rename rule subscription"
        text == "例如：EasyList China" -> "Example: EasyList China"
        text == "感谢每一位支持项目的朋友！" -> "Thanks to everyone who supports the project!"
        text == "请作者喝杯蜜雪 🧋" -> "Buy the author a drink 🧋"
        text == "如果这个项目帮助到了你，欢迎请作者喝杯蜜雪。" -> "If this project helped you, consider buying the author a drink."
        text == "付款时请备注您的网名或希望展示的名称，方便将您的名字加入赞助者名单。" -> "Please include your username or preferred display name with your payment so it can be added to the sponsor list."
        text.startsWith("感谢每一位支持谛听项目") -> "Thanks to everyone who supports Ting! Sponsors are listed from earliest to latest by default; use the button above to reverse the order. The order is unrelated to donation amount, and every contribution is equally appreciated."
        text == "代理地址仅支持 127.0.0.1 或 ::1" -> "The proxy address must be 127.0.0.1 or ::1"
        text == "代理端口必须在 1 到 65535 之间" -> "The proxy port must be between 1 and 65535"
        text == "请选择提供本地代理端口的应用" -> "Choose the app that provides the local proxy port"
        text == "不能选择 DNSSR 自身作为代理应用" -> "DNSSR itself cannot be selected as the proxy app"
        text == "所选代理应用未安装" -> "The selected proxy app is not installed"
        text == "代理账号或密码过长" -> "The proxy username or password is too long"
        text == "出站代理设置已保存" -> "Outbound proxy settings saved"
        text == "其他设置" -> "Other settings"
        text == "新增 DNS 服务商" -> "Add DNS provider"
        text == "添加自定义解析服务" -> "Add a custom DNS service"
        text == "设置自定义 DNS 服务的名称" -> "Set the name of a custom DNS service"
        text == "设置 DoH 请求地址" -> "Set the DoH request URL"
        text == "设置 DoT 服务器地址" -> "Set the DoT server address"
        text == "设置 DoT 服务端口" -> "Set the DoT service port"
        text == "管理内置解析 IP" -> "Manage built-in resolution IPs"
        text == "添加和管理自定义解析 IP" -> "Add and manage custom resolution IPs"
        text == "设置自定义 Bootstrap IP 名称" -> "Set a custom Bootstrap IP name"
        text == "填写自定义 Bootstrap IP" -> "Enter a custom Bootstrap IP"
        text == "启用或关闭本地响应缓存" -> "Enable or disable the local response cache"
        text == "跟随上游 TTL" -> "Follow upstream TTL"
        text == "最长 1 小时，短 TTL 至少 1 分钟" -> "Up to 1 hour; short TTLs are at least 1 minute"
        text == "最长 6 小时，短 TTL 至少 2 分钟" -> "Up to 6 hours; short TTLs are at least 2 minutes"
        text == "仅切换阿里云和 DNSPod 内置服务的 DNS、DoT 或 DoH 协议" -> "Switch the DNS, DoT, or DoH protocol of built-in Alibaba Cloud and DNSPod services"
        text == "配置候选服务，按近期成功率和延迟优先选择" -> "Configure candidate providers and prioritize recent success rate and latency"
        text == "配置同时查询并采用最先成功结果的服务" -> "Configure providers queried in parallel and use the first success"
        text == "配置失败后依次尝试的服务顺序" -> "Configure the provider order used after failures"
        text == "设置日志记录范围" -> "Set the logging scope"
        text == "设置日志保留天数" -> "Set the log retention period"
        text == "后台显示悬浮球并查看最近请求" -> "Show a floating button in the background and view recent requests"
        text == "隐藏最近任务卡片并禁用任务截图" -> "Hide the recent-task card and disable task screenshots"
        text == "VPN 未运行时在通知栏常驻提醒" -> "Keep a notification reminder while the VPN is not running"
        text == "前往系统电池优化设置" -> "Open system battery optimization settings"
        text == "导入或导出名称、协议和解析地址" -> "Import or export names, protocols, and resolution addresses"
        text == "导入或导出名称、IP 和启用状态" -> "Import or export names, IPs, and enabled states"
        text == "导入或导出订阅名称和链接" -> "Import or export subscription names and URLs"
        text == "导入或导出使用系统 DNS 的应用包名" -> "Import or export package names of apps using system DNS"
        text == "将勾选内容保存为 JSON 配置文件" -> "Save selected items as a JSON configuration file"
        text == "合并配置并跳过本机已有项目" -> "Merge configuration and skip items already on this device"
        text == "清除 DNS 和 HTTP 的历史请求记录" -> "Clear historical DNS and HTTP request records"
        text == "移除已缓存的解析结果" -> "Remove cached resolution results"
        text == "清除竞速模式的健康样本" -> "Clear race-mode health samples"
        text == "清除 Bootstrap DNS 解析健康样本" -> "Clear Bootstrap DNS resolution health samples"
        text == "删除全部规则" -> "Delete all rules"
        text == "清除手动添加和订阅导入的所有域名规则" -> "Clear all manually added and subscription-imported domain rules"
        text == "删除全部域名规则" -> "Delete all domain rules"
        text == "删除全部地址规则" -> "Delete all address rules"
        text == "清除域名屏蔽、白名单、覆写规则及对应订阅" -> "Clear domain block, allowlist, override rules, and their subscriptions"
        text == "清除 URL 屏蔽和放行规则" -> "Clear URL block and allow rules"
        text == "让所有首次进入说明再次显示" -> "Show all first-visit guides again"
        text == "确定要删除所有本地 DNS 缓存吗？下次访问域名时会重新查询。" -> "Delete all local DNS cache? The next visit to a domain will query it again."
        text == "确定要删除所有 DNS、HTTP 请求日志、竞速统计和 Bootstrap DNS 解析统计吗？" -> "Delete all DNS and HTTP request logs, race statistics, and Bootstrap DNS resolution statistics?"
        text == "确定要清除所有服务商健康样本并恢复竞速模式默认权重吗？" -> "Clear all provider health samples and restore the default race-mode weights?"
        text == "确定要清除 Bootstrap DNS 解析健康样本并恢复默认权重吗？" -> "Clear Bootstrap DNS resolution health samples and restore the default weights?"
        text == "确定要删除全部 DNS 规则吗？域名屏蔽、白名单和覆写规则都会被移除。" -> "Delete all DNS rules? Domain block, allowlist, and override rules will be removed."
        text == "确定要删除全部 HTTPS 过滤规则吗？域名、URL、白名单和覆写规则都会被移除。" -> "Delete all HTTPS filtering rules? Domain, URL, allowlist, and override rules will be removed."
        text == "确定要重置所有应用设置新手引导和首次使用协议吗？这不会删除任何配置、规则、缓存、日志或证书。操作完成后应用将退出；下次打开时需要重新同意使用协议，所有新手引导也会再次显示。" -> "Reset all app setup guides and the first-use agreement? This will not delete configuration, rules, cache, logs, or certificates. The app will exit when complete; the agreement must be accepted again next time, and all setup guides will be shown again."
        text == "已恢复竞速模式默认权重" -> "Race-mode default weights restored"
        text == "已恢复 Bootstrap IP 默认权重" -> "Bootstrap IP default weights restored"
        text == "已重置所有新手引导" -> "All setup guides reset"
        text.startsWith("应用启动时不自动检查") -> "Do not check automatically at startup; you can check manually at any time"
        text.startsWith("应用启动时自动检查") -> "Automatically check for new versions at startup"
        text.startsWith("已选择 ") && text.endsWith(" 个应用") -> "${text.removePrefix("已选择 ").removeSuffix(" 个应用")} apps selected"
        text.startsWith("尚未选择应用") -> "No apps selected; traffic will not be blocked when enabled"
        text.startsWith("当前共有 ") -> text.replace("当前共有 ", "There are ").replace(" 条屏蔽规则", " blocked rules").replace(" 条白名单规则", " allowlist rules").replace(" 条 IPv4/IPv6 覆写规则", " IPv4/IPv6 override rules")
        text.startsWith("当前筛选下") -> text.replace("当前筛选下", "No records for the current filter")
        text.startsWith("成功 ") -> text.replace("成功 ", "Success ")
            .replace(" · 平均 ", " · Average ")
            .replace(" · 备用 ", " · Fallback ")
            .replace(" · 兜底 ", " · Fallback ")
            .replace(" · 样本 ", " · Samples ")
        text.startsWith("安装 ") -> text.replace("安装 ", "Install ")
        text.startsWith("下载 ") -> text.replace("下载 ", "Download ")
        text.startsWith("全部 ") && text.endsWith(" 服务") -> text.removePrefix("全部 ").removeSuffix(" 服务") + " services"
        text.endsWith(" 服务") -> text.removeSuffix(" 服务") + " services"
        text.startsWith("已删除") && text != "已删除" -> text.replace("已删除", "Deleted ")
            .replace("当前范围的全部规则", "all rules in the current scope")
            .replace("请求日志", "request logs")
            .replace("DNS 缓存", "DNS cache")
            .replace("全部规则", "all rules")
        text.startsWith("删除 ") -> text.replace("删除 ", "Delete ")
        text.endsWith(" 条规则") -> text.removeSuffix(" 条规则") + " rules"
        text.endsWith(" 个") -> text.removeSuffix(" 个")
        text.endsWith(" 条") -> text.removeSuffix(" 条")
        else -> text
    }
}

@Composable
fun localizedText(text: String): String {
    val configuration = LocalConfiguration.current
    val context = androidx.compose.ui.platform.LocalContext.current
    return if (configuration.locales[0].language == "en") translateEnglish(text, context) else text
}

fun localizedText(context: Context, text: String): String {
    val localizedContext = AppLanguageManager.wrap(context)
    return if (localizedContext.resources.configuration.locales[0].language == "en") {
        translateEnglish(text, localizedContext)
    } else {
        text
    }
}
